# 优化落实审查

审查日期：2026-09-06。依据当前未提交工作区，对照 [优化.md](优化.md) 的 R01～R40 及其验收要求。实现范围包括 Popup 故障和下列六项明确缺陷；其他条目继续保留落实状态，不把局部修复等同于 40 项整体交付。原方案中的历史交付记录保持原意。

**结论：没有全部改到位。** TextBox 同值赋值、Connection 源先销毁、数字显示/解析、表格增量失效和部分动画已有实质修改，但“有 API/有局部测试”不等于整项验收。以下“部分”包含核心修复已落地但配套契约、边界或业务迁移未完成的情况；“未落实”指本条要求的新增工作未找到，既有基础能力仍然存在。

## 六项明确缺陷的修复结果

用户确认后，以下问题已修复并加入回归；对应的完整优化条目仍可能有功能或专项验收未完成。

1. **R01：卸载门禁已修复。** [线程管理](src/core/app.cpp) 持有线程并在系统句柄确认退出后 join、回收，包含工作闭包与 TLS 析构。线程创建失败回滚登记，新任务启动时顺便回收已退出线程。测试专门让 TLS 析构阻塞，确认任务结果已产生时 CanShutdown 仍为 false。TaskHandle 的完整结果交付协议仍属后续工作。
2. **R04：模型源先销毁已修复。** [FilteredModel / SortedModel](include/lumen/ItemsModel.h) 订阅 OnDetached，清源指针和映射并发布 Reset；[Table](src/controls/table.cpp) 同时清类型访问闭包、取消编辑、清空行。回归覆盖嵌套装饰器通知视图及源销毁后的再次访问。
3. **R14、R34：NumberBox 草稿步进和 UIA 提交已修复。** [NumberBox](src/controls/number_box.cpp) 从合法草稿步进，非法草稿回退最近提交值；同值/边界不重复通知。RangeValue 和文本 Value 两种 UIA 入口共用有效数字提交，绑定同步；非有限值和非法文本拒绝。Gallery 实际输入草稿 4 后按上键得到 4.5（步长 0.5），超范围草稿按上限钳制。
4. **R18：类型化数字转换已增加校验。** [Table](include/lumen/Table.h) 整数拒绝小数和不可表示值，使用开区间上界避免 int64/uint64 上限转 double 的舍入陷阱；float 成员拒绝溢出。编辑器拒绝后保留草稿，切换单元格也不能绕过。数字通道仍基于 double，超过 2^53 的整数精确编辑不是本次提供的能力。
5. **R27：隐藏祖先/窗口动画已修复。** [调度](src/core/window_impl.cpp) 沿祖先判断可见性，隐藏目标暂停保留但不请求后续帧；重新显示的首帧恢复，避免丢失动画目标或累计隐藏时长。测试覆盖父隐藏、窗口隐藏、停止请求帧与恢复。
6. **R34：ComboBox UIA 折叠已修复。** [AutomationCollapse](src/controls/combo_box.cpp) 实际关闭属于当前 ComboBox 的 transient，并报告真实状态，不关闭其他控件的弹层。实窗自动回归覆盖 Expand→Collapse 和状态变化。

## R01～R40 对照

| 编号 | 状态 | 当前证据与剩余工作 |
| --- | --- | --- |
| R01 | 卸载门禁已修，整项部分 | [线程管理](src/core/app.cpp) 已等待实际线程退出、回滚创建失败并回收资源；TLS 析构门禁回归通过。TaskHandle 的工作终态与结果交付/取消协议仍需继续完善。 |
| R02 | 部分 | [PostPort](src/core/window_impl.h) 有独立共享状态、锁和 Accepted/Closed/WakeFailed；公共跨线程入口仍依赖 Window 对象存活，未提供应用可持有的独立投递句柄。缺唤醒失败注入、关闭竞态和大量投递验收。 |
| R03 | 核心修复已落地，整项部分 | [Signal](include/lumen/Signal.h) 的共享生存令牌保护源先销毁和移动；相关行为测试通过。但 Emit 拷贝回调快照，大捕获或多订阅仍可能分配；发射期间退订/销毁后剩余快照回调的语义需明确。 |
| R04 | 明确生命周期缺陷已修并回归 | [ItemsModel](include/lumen/ItemsModel.h) 装饰器处理源 detached；[Table](src/controls/table.cpp) 清类型闭包、取消编辑。嵌套装饰器源先销毁和表格再次访问均通过。 |
| R05 | 部分 | 有线程断言、宿主 API 和 [用法说明](.cursor/skills/lumen/references/use.md)，[examples](examples) 仍只有普通模板和 Gallery；缺独立 owned-window / embedded-child 宿主模板及对应消费验收。 |
| R06 | 本次修复核心故障，整项部分 | [PopupWindow](src/core/popup_window.cpp) 已补锚点跟随、正确坐标、关闭/重入/销毁清理和回归。超高内容仅限制高度，没有自动滚动容器；跨屏 DPI、复杂编辑器 IME、完整宿主交互未验收。 |
| R07 | 未落实 | [Window 公共头](include/lumen/Window.h) 未形成 Hide/Shown/Hidden/Activated/Destroyed 的高层事件契约，仍主要依靠原生消息。 |
| R08 | 部分 | [Table](include/lumen/Table.h) 已有 ColumnPixelWidth / ColumnsPixelWidth / NaturalHeight 并有 API 测试；窗口内容适配模式、滚动条查询和应用尺寸副本删除未完成闭环。 |
| R09 | 部分 | [SettingsCard](src/controls/settings_card.cpp) 测量/排列已预留尾部宽度；仍强制文字最小 60 DIP，极窄/多尾控件时可能相交，没有窄宽换行策略及完整宽度矩阵。 |
| R10 | 未落实 | 既有 WrapPanel/Grid 可用；[布局示例](examples/gallery/section_layout.cpp) 未提供本条要求的共享标签、自适应字段列、固定页脚模板及三种目标宽度验收。 |
| R11 | 未落实 | [公共接口](include/lumen) 未找到合并布局与模型通知的更新作用域及异常恢复协议。现有失效合并不能替代本条批量更新契约。 |
| R12 | 核心修复已落地，整项部分 | [TextBox::Text](src/controls/text_box.cpp) 同值早退，选区行为测试通过；文档重载/编辑冲突语义、真实中文组合输入中的外部更新尚未验收。 |
| R13 | 部分 | [事件契约表](.cursor/skills/lumen/references/use.md) 已区分现有 setter 行为；NumberBox UIA 已补齐，所有控件的程序赋值/编辑/提交协议与事件来源矩阵尚未完成。 |
| R14 | 明确解析/提交缺陷已修，整项部分 | [NumberBox](src/controls/number_box.cpp) 修复合法草稿步进、边界重复通知及非有限值；UIA 两种赋值均提交绑定，回归通过。空值/单位/更完整错误协议仍未提供。 |
| R15 | 部分 | [FormField](src/controls/form_field.cpp) 配置期挂钩及 ValidateAll 已实现；挂钩主要覆盖 TextBox/NumberBox，未覆盖选择字段、通用值适配与跨字段校验闭环。 |
| R16 | 未落实核心编辑契约 | [ComboBox](include/lumen/ComboBox.h) / [实现](src/controls/combo_box.cpp) 仍未形成可绑定文本、草稿提交、选区撤销、IME 和稳定 item key 的完整编辑能力。 |
| R17 | 部分 | [Control](include/lumen/Control.h) 的焦点事件、[TextBox](include/lumen/TextBox.h) 的选区事件和 Composing 查询已增加；完整事件来源、组合态变化通知及实机 IME 验收不足。 |
| R18 | 默认显示与转换校验已修，整项部分 | [类型列](include/lumen/Table.h) 数字默认文本、数值排序，整数及 float 表示范围已校验；编辑拒绝后保留草稿。枚举/空值和超过 2^53 的整数精确编辑未齐。 |
| R19 | 部分 | [Table 编辑](src/controls/table.cpp) 有 CellEditable、严格数字解析、取消和 Tab 导航；尚无完整列编辑器组合、枚举选择、行事务与批量粘贴验收。 |
| R20 | 部分 | [模型事件处理](src/controls/table.cpp) 已在排序/分组时重排、编辑期间延后、同数量 Reset 重排；稳定业务 RowKey、装饰器源行映射和选择保持未形成统一闭环。 |
| R21 | 部分 | [InvalidateRows](src/controls/table.cpp) 已局部失效行和页脚；SyncSlots 仍刷新可见池，批量通知/稳定 key 差分和逐行进度压力基准未完成。 |
| R22 | 仅有既有基础 | [Table](include/lumen/Table.h) 有宽度、显隐、冻结、排序能力；未形成稳定 ColumnKey、完整尺寸策略及版本化持久化流程。 |
| R23 | 部分 | [文本服务](src/core/text_service.cpp)、RichLabel、Table 字体能力已有改进；测量/绘制/命中/选区共用 TextRun 契约、工程数字对齐和同串跨控件验证仍不足。 |
| R24 | 部分 | [输入路由](src/core/input_router.cpp) 支持父链处理和横向滚轮；[Table](src/controls/table.cpp) 与 [ScrollViewer](src/controls/scroll_viewer.cpp) 在自身有可滚动范围时，即使到边界仍返回消费，边界传播及触摸板组合验收未完成。 |
| R25 | 部分 | [Segmented](src/controls/segmented.cpp)、[Drawer](src/controls/drawer.cpp) 已接主题时长和 MotionScale；尚无所有动画入口的映射/真实页面节奏验收，浮层仍有固定参数。 |
| R26 | 部分 | Segmented/Drawer 零动态效果吸附已实现；[浮层通道](src/core/overlay_host.cpp) 等所有入口、运行中切换减弱效果的完整矩阵未验收。 |
| R27 | 隐藏调度缺陷已修，整项部分 | [TickAnimations](src/core/window_impl.cpp) 检查祖先/窗口可见性，隐藏暂停不请求帧、显示后恢复，回归通过；所有控件的快速中断与终帧完整矩阵仍待补齐。 |
| R28 | 未落实完整规格 | [SettingsCard](src/controls/settings_card.cpp) 仍无条件绘制左侧辉光条；未找到工程页紧凑层级模板及真实页面视觉对照。 |
| R29 | 部分 | [PageHost](src/controls/page_host.cpp) 保留页面控件树，已有状态能随树保留；未实现完整焦点/滚动恢复、草稿离开策略和重建情形验收。 |
| R30 | 部分 | 现有 [键盘路由](src/core/input_router.cpp) 有浮层/焦点处理，本次修复 Popup Esc；尚未建立 IME→编辑器→浮层→窗口→宿主的统一优先级及目标宿主验证。 |
| R31 | 部分 | TaskHandle、Busy/InfoBar/Toast 基础存在；[状态示例](examples/gallery/section_status.cpp) 未完成阶段、取消仍执行、持久错误、失败行和重试的全部状态闭环。 |
| R32 | 未落实 | [PalettePage](../SHOWBOX-LUMEN/src/Ui/Palette/PalettePage.cpp) 仍有 SearchBox 派生和 Clear 后重建列表，未迁移稳定 key 的虚拟化命令项及过滤状态保持。 |
| R33 | 部分 | 图标注册/绘制基础已有；未找到 PathField/FieldWithAction 完整模板，也未完成目录、图层、颜色三类业务字段迁移。参见 [公共头](include/lumen) 与 [Gallery](examples/gallery)。 |
| R34 | 两条明确 UIA 缺陷已修，整项部分 | [NumberBox](src/controls/number_box.cpp) RangeValue/Value 共用数字提交，[ComboBox](src/controls/combo_box.cpp) 真正折叠；行为回归通过。表单标签/错误关联、完整表格 UIA 语义仍未齐。 |
| R35 | 未落实验收边界 | [Table::Draw](src/controls/table.cpp) 缓存重建仍 CreateCommandList，FooterText 在绘制中生成/聚合；未见 Draw 分配统计及静态/滚动/hover/长文本矩阵，不能宣称零堆。 |
| R36 | 部分 | [visual](tests/visual/main.cpp) 已有表格绘制稳定性、字体等回归且本次通过；要求的连续交互+DPI+隐藏重开+设备恢复全组合未全部覆盖。 |
| R37 | 未落实新增基准 | [perf](tests/perf/main.cpp) 仍是固定离屏场景；四类业务负载、输入到帧、Present、分位数、分配量和宿主资源趋势没有完整报告。 |
| R38 | 未落实关键修复 | [CMakeLists](CMakeLists.txt) 仍有目录级严格编译选项和 PUBLIC `/permissive- /utf-8`；缺 exe/MFC/ARX/安装 SDK 消费矩阵及移除应用清空接口选项的闭环。 |
| R39 | 部分 | README 编译/命名检查和技能行为表已有；[NumberBox 公共头](include/lumen/NumberBox.h) 等仍有泛化文案，默认尺寸、错误/事件/生命周期契约尚未逐控件补全。 |
| R40 | 未落实完整验收 | [Gallery](examples/gallery/main.cpp) 仍按控件分类，未形成四个完整业务场景和三项 SHOWBOX 迁移闭环；DumpTree 尚未覆盖约束、裁剪和布局触发原因等要求。 |

## 本次 Popup 修复

- 主窗移动、布局变化时重新定位；锚点失效、隐藏或 owner 关闭时收起。鼠标坐标按消息来源转屏幕坐标后再转 Popup 局部坐标。
- 每 UI 线程仅一个阻塞会话，忽略重入请求，避免叠出窗口。外点、Esc、capture 丢失及主动关闭走统一清理；主动关闭立即隐藏。
- 借用内容通过弱引用跟踪，返回时解绑；平衡 Renderer 浮层状态。owner 销毁后不执行可能捕获旧对象的 closed 回调；保留 WM_QUIT 和退出码。
- 增加 Popup 内焦点/Tab、键盘/字符及滚轮路由，统一滚轮单位。复杂编辑器的程序 Focus/IME 等仍不列入已验收能力。

主要文件：[popup_window.cpp](src/core/popup_window.cpp)、[popup_window.h](src/core/popup_window.h)、[window_impl.cpp](src/core/window_impl.cpp)、[Window.h](include/lumen/Window.h)、[回归测试](tests/visual/main.cpp)。保留工作区原有其他修改。

## 验证与边界

### 六项缺陷修复后的最终回归

- `build.bat` 成功，无 C/C++ 编译警告；visual / perf / anim / api 四个程序退出码均为 0，visual/anim ALL PASS，API 行为断言通过。
- 性能：相同固定离屏场景平均 **1.645 ms/帧**、最差 **2.610 ms/帧**，满足 <8 ms；测试时 Gallery 已关闭。
- 新增用例覆盖：线程 TLS 析构期间的卸载门禁、嵌套装饰器源先销毁、NumberBox 草稿/边界/UIA 两种赋值、表格整数和 float 越界/无效草稿保持/源销毁、ComboBox 实际折叠、隐藏动画停止与恢复。原有 Popup 和 12 轮 HostCycle 同时通过。
- Gallery 实际操作验证数字草稿步进、范围钳制和输入页滚动。UIA、隐藏动画和模型销毁由自动回归验证；IME、读屏、跨屏 DPI、AutoCAD 加载卸载仍未实机验收。
- 最终日志：`%TEMP%/lumen-audit-{build,visual,perf,anim,api}.log`。公共头和技能源/分发副本的相关契约已同步更新。

### 前一轮 Popup 修复记录

- `build.bat` 成功，未出现 C/C++ 编译警告；配置阶段存在依赖项的 CMake 弃用提示。
- `lumen_visual_test.exe`：ALL PASS。新增 Popup 回归覆盖主窗移动、正确命中、立即隐藏、重入、外点、Esc、capture 丢失、隐藏/失活、WM_QUIT、锚点删除、owner 销毁与滚轮单位；原有 12 轮 HostCycle 也通过。
- `lumen_perf_test.exe`：平均 **1.615 ms/帧**，最差 **2.219 ms/帧**，满足 <8 ms。场景为 1280×800、8 按钮、100,000 行虚拟列表、聚光卡、Area/Heatmap，全帧重绘 300 帧；这不是 AutoCAD 交互延迟。
- `lumen_anim_test.exe`：ALL PASS。`lumen_api_test.exe`：退出码 0，行为断言通过；构建同时完成命名和 README 编译检查。
- Gallery 实际窗口：打开 Overlays 色板，看到独立 Popup，实际点击灰阶后窗口消失并显示关闭反馈。后续检查时 Gallery 已不在运行窗口列表，未将拖动、缩放或完整 Tab/DPI 手感写成实机通过。上述主窗移动等结果来自自动消息回归。
- 差异空白检查通过。技能源和分发副本已同步核对，报告相对链接已检查。

日志保存于 `%TEMP%/lumen-popup-{build,visual,perf,anim,api}.log`。自动消息回归与实际鼠标操作分开计算，不用启动 Gallery 代替完整实机检查。

未执行：AutoCAD 加载卸载/停靠/文档切换，真实 IME 候选、读屏、触摸板手感，双屏不同 DPI 拖动，以及 R37/R40 要求的业务性能和迁移验收。报告中的其他缺口主要依据源码审查，不冒充已逐项复现的运行故障。
