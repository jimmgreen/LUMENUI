# LUMEN 优化方案：从 SHOWBOX 实际项目反推库的下一步

> 2026-09-06 落实复核与 Popup 修复结果见 [优化落实审查.md](优化落实审查.md)。下文保留 2026-09-05 的方案与历史交付记录，不能据此认定 40 项已经完成。

审查日期：2026-09-05。对象：`.` 与 `../SHOWBOX-LUMEN` 的当前工作区源码，包含尚未提交的修改。

本次交付为优化设计与代码审查文档，未修改库或 SHOWBOX 的实现，未重新构建、运行测试或在 AutoCAD 中操作界面。下文“已确认”指源码事实与可推导的调用路径；“历史已修”依据当前源码和 SHOWBOX 的修复记录；视觉感受、真实鼠标手感、IME 与宿主运行结果仍须实机确认。历史报告中的测试成绩不算本次执行结果。

## 1. 核心判断

LUMEN 已经有相当完整的控件、主题、虚拟化、动画、绑定和宿主接入基础。当前最值得投入的方向，是让这些能力在复杂业务页中形成稳定、连贯的使用方式。

SHOWBOX 的 UI 目录有 91 个 `.h/.cpp` 文件，其中 44 个 `.cpp`。按 `syncing_|updating_|rebuilding_` 搜索，24 个实现文件包含同步或重建保护标志；按 `Property<|BindText\(|BindValue\(` 搜索，44 个实现文件中没有直接命中。项目已经使用 `Form`、`FormField` 和表格模型，所以这不是“应用完全没有使用框架能力”。这些统计也不能独立证明绑定难用，但结合 setter 通知差异、编辑态被覆盖和手工同步代码，说明现有绑定还没有成为项目最省心的默认路径。

有代表性的实际负担：

| 实际页面/封装 | 已看到的使用成本 | 应沉淀的库能力 |
| --- | --- | --- |
| 钢筋纵筋选配页 | 计算表格自然尺寸、复制表头/滚动条常量、维护选区与焦点恢复、缓存上次模型 | 内容尺寸、编辑状态、模型更新协议 |
| 层高表 | 文本列模拟数值与枚举编辑，换行时手工提交、格式化、联动计算 | 有类型的单元格、编辑事务、校验 |
| 批量打印页 | 全表快照转换和 Reset、手动抽屉/忙碌状态、目录选择组合 | 增量更新、任务状态、复合字段 |
| 命令面板 | 每次搜索清空重建分组，派生 TextBox 只为收到焦点事件，自定义图标控件 | 可组合命令项、焦点事件、搜索模型 |
| AcadColorPicker | 自建无标题顶层窗口、位置换算、工作区钳制、owner subclass | 可越过客户区的通用 Popup |
| WindowHost / UiThread | 投递队列、生命周期代次、关闭门禁、隐藏复用、延迟调整尺寸 | 明确的任务与窗口生命周期协议 |

### 优先顺序

1. **P0：生命期安全。** 先处理后台任务、投递和订阅对象先销毁的问题。这些问题会直接影响宿主进程稳定性。
2. **P1：输入、数据和布局正确性。** 优先修文本重复赋值、数字校验、表单、表格编辑与设置卡文字区域。
3. **P1：统一动效行为。** 先解决现有控件不一致、关闭动画不完全生效和父层隐藏后的动画收尾，再加新效果。
4. **P2：界面效率与复合控件。** 用真实页面推动紧凑密度、命令面板、路径字段和布局模板。

本文共 40 项。每项标明优先级、事实依据、改法和验收。新 API 名均为**设计候选**，不是当前已存在的接口；最终命名仍须遵守仓库的链式属性与 `OnX/BindX` 约定。

## 2. 已修能力应固化，不能重新列成“待实现”

对照 [SHOWBOX 修复记录](../SHOWBOX-LUMEN/优化.md) 与上游当前实现，下列能力已经存在：

| 已有能力/历史修复 | 后续重点 |
| --- | --- |
| Table 弹性列、横向滚动、列显隐通知 | 公开自然尺寸与实际列宽，补齐组合场景 |
| `Window::MeasureContent(clientWidth)` | 建立“测量→工作区约束→布局→显示”的完整策略 |
| 表格文字裁剪、文字对齐与绘制稳定性修复 | 把鼠标移动、字体回退、DPI、滚动交叉覆盖进回归 |
| `HostMode`、模块句柄、DPI 上下文与窗口类清理 | 做官方嵌入样例，减少业务对内部约束的记忆成本 |
| `ClearFocus`、WM_KILLFOCUS 清理、原生消息订阅 | 补高层焦点/显示事件，收敛应用 subclass |
| LumaText 延迟加载、拥有引用、卸载与重启 | 验证其他插件共存、失败重试和多窗口关闭 |
| UIA 失效、断开重试、`CanShutdown` 门禁 | 补控件行为一致性与真实读屏客户端验证 |
| Tween、SpringMotion、motion_scale、Density、ThemeOverride | 审查所有控件是否一致消费这些能力 |
| Acrylic、Elevation、ScrollViewer 锚定、PageHost | 继续完善现有实现，不重复造一套同名系统 |
| List/Table 可见项虚拟化、模型逐项通知 | 补稳定标识、排序更新和真实高频数据场景 |

旧版 LUMEN 优化稿中的“全帧立即重绘”“尚无 Acrylic/海拔”“0.17 ms 当前基线”等描述不能原样继承。当前库已有脏区、缓存与 Acrylic；不同文件和历史记录中的性能数字对应不同场景，应按第 37 项统一测量口径。

## 3. 生命周期与宿主接入

### R01 · P0：RunAsync 必须有可关闭、可失败的任务协议

**已确认：** [WindowImpl::RunWorker](./src/core/window_impl.cpp:1456) 创建 detached thread，捕获裸 `this`；`work()` 外没有异常处理；工作结束后先检查 `alive.expired()` 再调用 `Post()`。检查与访问之间窗口仍可能析构，弱标志并未拥有 `WindowImpl`。未捕获的工作异常会进入线程的异常终止路径。`App::CanShutdown()` 当前只查询 UIA，不能据此推断后台任务已退出。

**优化：** 独立于窗口对象保存任务/调度状态，工作完成只通过仍有效的投递端口提交结果；返回任务句柄，区分完成、失败、取消请求与结果被丢弃。异常进入错误回调，忙碌遮罩在所有完成分支收尾。模块卸载必须等仍执行模块代码的任务结束，或明确返回不能卸载；不能用“丢弃 UI 回调”代替任务结束。

**边界：** CAD 操作继续由 SHOWBOX 的 CAD 调度器执行，不能搬进普通工作线程。取消是协作协议，不能承诺强制中止任意函数。

**验收：** 工作抛异常、窗口先销毁、连续关闭重开、结果晚到、卸载时仍有任务；无悬空访问，错误可见，每个任务恰有一个终态。

### R02 · P0：Post 要能报告拒绝，并把唤醒和生命期一起管理

**已确认：** [WindowImpl::Post](./src/core/window_impl.cpp:1778) 入队后调用 `PostMessageW`，没有处理返回失败；公共 `Window::Post` 返回 `void`。队列互斥保护了容器，未给窗口对象本身提供跨线程存活保证。SHOWBOX 为此类需求自行实现了事件唤醒、接收门禁与队列停止协议，见 [UiThread.cpp](../SHOWBOX-LUMEN/src/Ui/UiThread.cpp)。

**优化：** 引入可独立持有的 UI 投递端口，返回“已接收/已关闭/唤醒失败”；明确同线程是否始终排队。按队列从空变为非空合并唤醒，失败有回滚或补唤醒方案。关闭先关接收门，再完成/取消剩余任务；避免要求调用者在任意线程持有裸 Window 指针。

**验收：** 注入唤醒失败、关闭期间投递、同线程嵌套投递、队列洪泛；不能永久挂起，也不能在返回拒绝后又执行该任务。

### R03 · P0：Connection 应允许事件源先销毁

**已确认：** [Signal.h](./include/lumen/Signal.h:41) 中 Connection 保存事件源裸指针，析构调用 `Signal::Disconnect`；Signal 析构没有使外部 Connection 失效。若页面保存连接，而对应控件先因 `Clear()` 被销毁，连接稍后析构便可能访问已释放的源。RAII 目前只覆盖“订阅者先走”的安全顺序。

**优化：** 在事件源销毁时失效所有连接，或用独立连接状态验证源存活。优先评估侵入式连接链，控制分配与体积；不要求为每次 Emit 新建 shared_ptr。规定 Emit 中退订、销毁订阅者/事件源时，尚未执行槽是否继续执行；拷贝回调列表只能保证迭代容器稳定，不能自动保证捕获对象存活。

**验收：** 源先销毁、连接先销毁、移动连接、回调中退订/清空树；连接析构安全。追加大捕获 lambda 的分配检查，不把“std::array 放栈上”等同于 std::function 拷贝零分配。

### R04 · P0：模型装饰器的生命期必须与基础模型一致

**已确认：** [ItemsModel.h](./include/lumen/ItemsModel.h) 提供 `OnDetached`，Table 已订阅；FilteredModel、SortedModel 的 `BindSource()` 只订阅插入、删除、修改和重置，保留源裸指针。源先销毁时装饰器后续访问及连接析构都存在风险，后者还依赖 R03。

**优化：** 装饰器订阅源失效并清映射，提供清晰的借用/共享拥有入口；断开时通知下游模型变空。同步检查 `Table::Bind(VectorModel<T>&)` 保存的 typed_get/typed_touch 是否在脱离时清理，避免只清 model_ 却留下捕获旧模型的函数。

**验收：** `源→过滤→排序→表格` 中每一层分别先销毁，查询、换绑和销毁均安全；空源不再返回旧 Count。

### R05 · P1：提供宿主接入模板和线程诊断，保留业务边界

**依据：** [WindowHost.h](../SHOWBOX-LUMEN/src/Ui/WindowHost.h)、[UiThread.cpp](../SHOWBOX-LUMEN/src/Ui/UiThread.cpp)、[LumenPaletteHost.cpp](../SHOWBOX-LUMEN/src/Ui/Palette/LumenPaletteHost.cpp) 已实现线程归属、任务代次、模态恢复和嵌入分阶段清理。它们是可提炼的实战材料，但不能整套搬入核心库。

**优化：** 官方增加 host-window 和 embedded-child 样例：建窗、外部消息泵、owner、DPI、隐藏复用、拒绝卸载、销毁顺序。调试版在控件树变更、布局、焦点、模型通知等入口校验所属 UI 线程，日志带控件/窗口标识与调用阶段。提供同步事务的通用能力，不引入 AutoCAD 类型、文档锁或全局快捷键策略。

**验收：** 从模板接入的 DLL 宿主可连续建窗/关闭/Shutdown 至少 12 轮；错误线程有可定位诊断；宿主仍决定何时禁用主窗、恢复焦点和允许卸载。

### R06 · P1：通用 Popup 支持越过客户区，并复用现有浮层基础

**已确认需求：** [AcadColorPicker.h](../SHOWBOX-LUMEN/src/Ui/AcadColorPicker.h) 明确说明自建顶层小窗是因为 Flyout 被父客户区裁剪；[实现](../SHOWBOX-LUMEN/src/Ui/AcadColorPicker.cpp) 手工管理定位、工作区和隐藏监听。LUMEN 已有菜单/下拉窗口，应先从这些通道提炼。

**优化：** 让通用弹出内容明确选择窗口内浮层或独立 Popup；统一锚定、向上/向下翻转、可用高度滚动、跨 DPI 坐标转换、owner 隐藏/销毁收起、点击外部关闭和焦点恢复。Popup 使用库内部窗口通道，控件无需再写 Win32 subclass。

**验收：** 紧贴屏幕四边、不同 DPI 双屏、同页两个色板、嵌入面板收起、锚点销毁、Esc；菜单与色板的嵌套关闭顺序明确。

### R07 · P1：区分关闭、隐藏、激活和销毁，给应用可订阅事件

**依据：** [WindowHost.cpp](../SHOWBOX-LUMEN/src/Ui/WindowHost.cpp:513) 通过 OnClosing 否决实际关闭，再用 ShowWindow 隐藏；钢筋页和颜色选择器还需要监听原生窗口消息。库目前已有原生消息订阅，但常见生命周期仍需应用自行解释。

**优化：** 补 `Hide`、显示/激活状态、显示前准备与隐藏后收尾等高层接口；明确 Close 是请求还是同步销毁，Destroyed 与 Closing 分开。隐藏停止非必要动画、收起关联弹层、处理焦点；复用重开恢复草稿或重新加载由页面策略决定。

**验收：** 标题栏、Esc、程序隐藏、owner 隐藏、真实销毁五条路径分别验证；页面收尾不重复，隐藏不误触发“已取消业务提交”。

## 4. 布局、窗口尺寸与编辑状态

### R08 · P1：控件应公开内容尺寸，应用不再复制内部常量

**已确认：** [RebarDesignPage.cpp](../SHOWBOX-LUMEN/src/Ui/RebarDesignPage.cpp:255) 已调用 MeasureContent，却仍依赖表头 32、横向滚动条 10、弹性列最小 96 DIP，手算自然表高和工作区。`Table::ColumnWidth()` 返回声明宽，弹性列是 0；表头/行高相关查询有的在 protected 区。

**优化：** 为 Table 提供内容期望尺寸、实际列宽、可见列总宽与滚动条占用的只读查询。窗口内容适配支持“首次按内容”“用户缩放后保持”“显式重新适配”，用实际目标显示器限制尺寸。保留 MeasureContent 的不显示、不移动窗口约定，并明确其是否允许触发测量期间的订阅/校验副作用。

**验收：** 1/12/40 行、14 个直径列、显隐与拖宽、不同密度、100%/150%/200% DPI；大内容有滚动，页脚可达，重复测量稳定，页面不再出现库内部尺寸副本。

### R09 · P1：修正 SettingsCard 的测量/排列文字区域

**已确认：** [settings_card.cpp](./src/controls/settings_card.cpp:9) 测量时从可用宽度扣除尾部控件；排列时 x 在排完所有尾部控件后才计算 text_width_，未保留尾部起点，导致绘制文字区域可延伸到尾部操作区。Measure、Arrange、Draw 对文字起点和间距也分别计算。

**优化：** 一次求出 glyph/text/tail 三个矩形，测量与绘制共享；窄宽时允许尾部操作移到下一行，标题截断或换行策略显式。超长描述不能覆盖 Switch，也不能依靠交互子控件的碳底遮盖错误文字区域。

**验收：** 无/有图标、短/长标题、中文多行说明、多个尾部按钮，240/320/520 DIP 宽度；文字与尾部区域不相交，测量高度足够。

### R10 · P1：增加面向设置页的自适应布局策略

**依据：** [PlotBatchPage.cpp](../SHOWBOX-LUMEN/src/Ui/PlotBatchPage.cpp) 多组横排字段，最小宽度 1080；[SwColumnSettingsPage.cpp](../SHOWBOX-LUMEN/src/Ui/SwColumnSettingsPage.cpp) 有大量手动 Row/Column 组合。库已经有 WrapPanel、Grid 和 Grow，应在它们之上补常用表达。

**优化：** 支持按字段最小宽度自动换列、标签共享宽度、宽屏双列/窄屏单列、固定页脚+滚动正文模板。把 MinSize 的硬约束与首选尺寸分开；当屏幕比最小窗口更小时，明确哪些区域可滚动，避免整个窗口移出工作区。

**验收：** 320 DIP 嵌入栏、680 DIP 设置窗、1280 DIP 打印窗，长中文和长路径都能到达；不通过减小到难读字号解决溢出。

### R11 · P1：批量更新应有作用域，合并布局和通知

**依据：** [WindowHost::requestUiUpdate](../SHOWBOX-LUMEN/src/Ui/WindowHost.cpp:972) 必须始终排队以避免持锁同步重入；多个页面用 syncing_ 包住大批 setter。许多 setter 每次调用 RelayoutParent/Invalidate。

**优化：** 设计 UI 更新作用域，合并布局失效、模型通知和最终绘制；异常时自动恢复。明确作用域只控制发布时机，不吞掉业务变化，也不跨 CAD/工作线程持锁。优先让同值 setter 成为 no-op，再提供批处理，不以全局“禁事件”掩盖循环绑定。

**验收：** 同一页连续更新 100 个字段只发生必要的最终布局；回调中请求下一次更新不会同步重入；异常后后续事件仍正常。

### R12 · P1：TextBox 同值赋值不能破坏正在编辑的状态

**已确认：** [TextBox::Text](./src/controls/text_box.cpp:231) 无同值短路，每次 ClearCompose、把 caret/anchor 放末尾、清滚动位置与撤销栈。钢筋页已经在 [sync](../SHOWBOX-LUMEN/src/Ui/RebarDesignPage.cpp:1143) 手工比较文本并避开聚焦输入框。

**优化：** 同值立即返回；分清“重置整个编辑文档”和“同步外部值”。外部值与用户草稿冲突时用版本/来源策略处理，不默认覆盖 IME 组合。需要有意清撤销或重置选区时用明确动作，不让普通属性写入暗含全部副作用。

**验收：** 光标在中间、选区非空、已撤销一次、横向滚动、IME 组合中反复同步同值，状态保持；真正换文档仍能明确重置。

### R13 · P1：统一程序赋值、用户编辑和提交的事件语义

**已确认：** TextBox::Text、NumberBox::Value、Switch::Checked 编程赋值静默；ComboBox::SelectedIndex、Segmented::SelectedIndex 在变化时发事件。这使页面很难仅凭属性命名判断是否需要 syncing_。两个 ptrdiff_t 参数在 Table 中表示视图行/数据行，不能误当通用 old/new。

**优化：** 写出所有控件的事件契约表；区分值变化、用户编辑、提交、取消及来源。绑定内部可传播属性变化，业务提交只订阅提交事件。若统一现有 setter 行为需要破坏性变更，提供明确版本与迁移清单，同步更新 SHOWBOX，不保留旧 Set/Get 别名。

**验收：** 程序赋值、鼠标、键盘、绑定、UIA 分别覆盖；同值不重复通知；双向绑定无递归，业务保存不因初次 sync 被意外执行。

### R14 · P1：NumberBox 要可靠处理非法值、草稿和提交

**已确认：** [number_box.cpp](./src/controls/number_box.cpp) TryParse 使用 wcstod，未检查有限性/溢出；非空非法文本失焦回落 min，空文本失焦跳过提交；↑↓和 spin 走 ValueChanged，普通输入主要走文本事件。[层高表](../SHOWBOX-LUMEN/src/Ui/FloorHeightTablePage.cpp) 的应用解析还只检查 stod 消费了部分字符，暴露了统一数字输入的价值。

**优化：** 统一严格解析器：完整消费、有限值、范围、精度、空值策略；区分编辑中的 `-`、`1.` 与已提交非法文本。支持 Nullable、单位后缀、整数模式、提交时机和显式校验错误。默认不要悄悄把非法工程参数保存成下界；范围修正策略由调用者明确选。尺寸单位/范围列表/钢筋语法解析留在领域层。

**验收：** 空、负号、小数、指数、超范围、NaN/Inf、`12abc`、粘贴、只读、输入后直接按确定；格式与事件结果一致，错误文本保留供修正。

### R15 · P1：Form 的有效性不能依赖下一次布局或仅用户打字

**已确认：** [FormField::EnsureHooked](./src/controls/form_field.cpp:79) 在 Measure 中查找直接 TextBox 子级并监听 TextChanged；程序 Text()/NumberBox::Value() 不发该事件。首次 hook 后程序赋值、数字步进等路径可能保留旧错误。Form::Valid 初始为 true，尚未测量前不能作为完整提交校验结果。

**优化：** 校验连接在字段配置/内容接入阶段建立；增加显式 ValidateAll/提交校验流程，支持值接口或外部验证适配器，覆盖数字、选择器和跨字段规则。Required 标记与 Required 规则的关系写清。区分 untouched、editing、invalid、valid、pending；异步规则用代次丢弃旧结果。正则在配置规则时编译一次，别每次输入重建。

**验收：** 初次显示前提交、加载非法默认值、代码由非法改合法、spin 修正值、条件隐藏字段、异步旧结果；提交状态与最终数据一致，校验不靠强制 LayoutNow。

### R16 · P1：Editable ComboBox 补齐真正的文本编辑契约

**已确认：** [ComboBox.h](./include/lumen/ComboBox.h) 有 Editable 与 SelectedText，但没有对应公开编辑文本 setter、TextChanged/提交契约；[OnChar](./src/controls/combo_box.cpp) 直接修改 edit_text_、清 selected_ 并打开过滤列表。不能把“选中项变了”等同于“用户输入了自定义值”。

**优化：** 基于现有文本服务/编辑通道实现可编辑区域，完整支持选区、撤销、粘贴、IME 与文本绑定；选中值、输入文本、匹配项三个状态分开。补稳定 item key 和按 key 选择，候选刷新时不靠中文显示文案反查下标。

**验收：** 预置非候选值、中文输入、清空后提交、重复标签不同 key、候选刷新、Esc 取消筛选；最终可读到用户实际输入，程序回填行为一致。

### R17 · P1：公开焦点与选区事件，减少纯转发派生类

**已确认需求：** [PalettePage::SearchBox](../SHOWBOX-LUMEN/src/Ui/Palette/PalettePage.cpp:15) 派生 TextBox 只为转发 OnFocusChanged；钢筋编辑器手工读取/恢复 SelectionStart/End，维护 pending focus/selection 标记。

**优化：** Control 提供链式/RAII 焦点变化订阅，TextBox 提供选区变化和组合输入状态查询/事件；明确鼠标、键盘、程序设置的来源。宿主只订阅“输入区需要键盘”状态，CAD 焦点转移仍在应用层处理。选区必须使用与文本编辑器一致的索引单位。

**验收：** 点击输入、Tab、点击 CAD、Popup 获焦、隐藏重开、选区拖动；事件不重复，事件观察本身不改变焦点与 IME。

## 5. 数据表格、模型与工程输入

### R18 · P1：Table 类型绑定不要把所有浮点数默认画成进度条

**已确认：** [Table::Column](./include/lumen/Table.h:55) 把 wstring 映射 TextBox、bool 映射 CheckBox、float/double 映射 Progress，其他类型进入 unsupported 分支。工程项目的标高、层高、比例、数量多数是数字，不是 0～1 进度。

**优化：** 显式列描述符区分 Text/Number/Enum/Check/Progress/Action；整数和浮点默认数字显示，是否可编辑单独声明。数字列具有格式、精度、空值、范围、对齐、单位与数值排序。可组合 getter/setter，保留动态列与现有低层 CellText 扩展入口。

**验收：** double 标高显示 `-3.600`，int 数量可编辑，Progress 仍显式绘制；排序 `2, 10, 100` 正确，string 展示列不会默认铺满常驻输入框。

### R19 · P1：表格编辑提供开始、校验、提交、取消完整事务

**实际需求：** [FloorHeightTablePage.cpp](../SHOWBOX-LUMEN/src/Ui/FloorHeightTablePage.cpp) 用 BindTextBox 编辑多个列，以 lastNumericEdit_ 记录最近数字列，在换行/运行 CAD 前 flushNumericEdits。材料等级实际是枚举，却只能按文本处理。库已有 CellEditEnabled/OnCellEdited，不必从零做行内编辑。

**优化：** 在现有编辑器复用机制上增加 Number/Combo 编辑器、逐单元格可编辑条件、拒绝提交与错误展示、行提交事务。Tab/Shift+Tab 移到下一个可编辑格，Enter 提交，Esc 撤回本次；粘贴 TSV 可预览校验并作为一次批量事务。结构计算公式由页面在成功提交后执行。

**验收：** 最后一行不可编辑格确实不可进入；同一行从标高切到层高也能正确提交；滚动导致编辑器复用、排序、删除当前行、中文组合中 Enter 均不丢输入。

### R20 · P1：排序、模型行、源行与业务标识必须分清

**已确认：** [Table::Bind](./src/controls/table.cpp:471) 的 OnChanged 和等行数 OnReset 路径调用 SyncSlots/Invalidate，没有在该路径重算排序/分组。源值变更后，排序箭头可继续显示原排序而数据次序保留旧映射。`DataRowAt()` 只映射 Table 自身 order_；ItemsModel 的 SourceIndex 又是另一层含义。

**优化：** 先为排序键修改、同数量重置补正确性回归；明确定义 ViewIndex、ModelIndex、SourceIndex 与稳定 RowKey。排序、过滤、插入删除后用 key 保持选择、编辑和滚动锚点。编辑时是否实时重排应可配置，默认可延后到提交，避免正在输入的行突然移动。

**验收：** 排序后改排序键、同数量替换数据、过滤+排序叠加、在选中行前插入一行；选择仍对应同一业务记录，点击与编辑不会落错源行。

### R21 · P1：高频更新要真正利用增量模型通知

**依据：** [PlotBatchPage::syncTableRows](../SHOWBOX-LUMEN/src/Ui/PlotBatchPage.cpp:594) 每次重建所有 UI 行并 Reset；[RebarDesignPage.h](../SHOWBOX-LUMEN/src/Ui/RebarDesignPage.h) 额外保存 lastMainRows_/lastSideRows_ 来避免同值 Reset。库已有 At/Inserted/Removed，缺少好用的批量差量路径与稳定身份。

**优化：** 先在样例中演示逐行 At 和批量通知，再评估按 key 合并快照的轻量工具；通知携带范围/受影响列。Table 可只同步可见且脏的 slot，不因单行进度更新刷新所有编辑控件。模型更新不能随意重置用户列布局、焦点和选择。

**验收：** 1 万行中每次只改一行，UI 工作量与变化范围/可见范围相关；打印状态更新时仍可输入、滚动与选择；记录更新耗时和分配量。

### R22 · P2：列尺寸、显隐、冻结与持久化形成完整使用流程

**依据：** [Table.h](./include/lumen/Table.h) 已有弹性列、冻结、列拖动/重排/显隐；[钢筋页](../SHOWBOX-LUMEN/src/Ui/RebarDesignPage.cpp) 借表头右键控制直径列。当前默认 0 宽均分、96 DIP 下限不能表达所有工程表的列约束。

**优化：** 支持每列 min/preferred/max、弹性权重、按内容测量与恢复默认布局；显隐提供可发现入口，不能只靠右键。布局持久化按 ColumnKey，与显示标题、列号解耦；新增/删除列时容错恢复。滚动条视觉厚度和鼠标命中宽度分别设计。

**验收：** 改中文标题后布局仍恢复；新增列不覆盖用户选择；全隐藏被拒绝；拖动后有恢复弹性布局入口；14 列在窄屏仍可横向到达。

### R23 · P2：混合字体和工程数字排版共用文本能力

**依据：** [UiResources](../SHOWBOX-LUMEN/src/Ui/UiResources.h) 注册钢筋符号字体；Table 已有 CellCharacterFont，RichLabel 已有富文本能力。应用需要普通中文、钢筋字形、数字同时清晰对齐。

**优化：** 在核心文本服务中统一轻量 TextRun/字体回退描述，测量、绘制、命中、选区共享同一结果；扩展到表格、标签、提示文本时不在各控件重新造字形拆分器。补数字等宽/小数点对齐、上/下标等需求的可行性验证，先做工程表实际用到的部分。

**验收：** 同一字符串在表格与详情处宽度/基线一致；自定义字体缺失、LumaText 回退、不同 DPI 下不裁钢筋符号，选择范围不按字形错误切断文本。

### R24 · P2：滚轮用途与嵌套滚动要可组合、可解释

**依据：** [RebarDesignPage.cpp](../SHOWBOX-LUMEN/src/Ui/RebarDesignPage.cpp:375) 把滚轮用于钢筋匹配选区切换，主/副表的 OnWheelScroll 都接入该逻辑；ScrollViewer 已有弹簧与锚定。

**优化：** 提供明确滚轮处理结果与边界传播规则；区分视口滚动、数值步进、业务切换，可通过修饰键或模式选择。高精度滚轮保留小增量，横向滚动不被业务纵向回调吞掉。表格与外层 ScrollViewer 到边界后按规则传递，而非双重滚动。

**验收：** 表格超高/超宽、嵌套滚动、触摸板细增量、输入框聚焦；用户能预知本次滚轮修改的是选区还是视口，业务切换不会无意使数据不可达。

## 6. 动效与界面语言

### R25 · P1：把各控件的动画参数统一接到已有 token

**已确认：** [Theme.h](./include/lumen/Theme.h) 已有 fast/normal/slow 和 ease；[Segmented](./src/controls/segmented.cpp:6) 仍固定 0.6 秒 Linear，[Drawer](./src/controls/drawer.cpp:19) 固定 0.22/0.18 秒，Expander 使用 duration_normal。这可能让同一页面中的切换响应节奏显得不一致，具体感受须在实机对比。

**优化：** 给状态反馈、选择指示器、展开、页面、浮层分别指定现有 token 的默认映射。建议起始调校值见下表；这是设计候选，须在真实页面比较后确定。逻辑状态立即更新，动画只表现变化；用户快速切换时从当前视觉位置继续。

| 场景 | 建议起始范围 | 行为 |
| --- | --- | --- |
| 悬停/焦点增辉 | 约 80～140 ms 的感知响应 | EaseTo，无悬停位移 |
| 按压反馈 | 约 60～100 ms | 中心微缩，释放迅速恢复 |
| Segmented/Tab 指示器 | 120～180 ms | 标准减速，取消 600 ms 线性拖尾 |
| Expander | 160～240 ms | 高度与箭头协调，反向可打断 |
| Drawer/Flyout | 入场 180～240 ms，退场 120～180 ms | 退场略快，完成帧精确到位 |
| 密集页数据刷新 | 默认不做整表位移 | 可用短暂局部亮度反馈 |

**验收：** 60/120/144 Hz 下以时间为准；连续点三次只落到最后目标，不等待旧动画播完。参数调整不更改布局/命中语义。

### R26 · P1：减少动态效果要覆盖每一个动画入口

**已确认：** `MakeTheme` 读取系统客户区动画设置，Control::EaseTo 与 PageHost 已处理 MotionScale；Segmented、Drawer 的直接 Tween::Play 路径未统一应用该因子。库已经具备减少动态效果基础，剩下的是完整接入。

**优化：** 统一“是否动画/缩放时长/直接 Snap”入口，覆盖 Drawer、Tab、Segmented、进度不定态、Toast、Acrylic 和滚动。提供窗口/应用显式 Reduced/Off 配置，便于宿主与测试控制；光效强度与动作幅度分开。不要为此恢复被禁止的亮色主题或系统强调色跟随。

**验收：** motion=0 时无多余位移、没有未完成 Tween 留在队列，焦点、选中、忙碌信息仍可见；关闭光效不影响文字和错误可读性。

### R27 · P1：动画调度检查祖先可见性，并保证中断与终帧

**已确认：** [TickAnimations](./src/core/window_impl.cpp) 在队列中直接检查 current->visible_；仅父页隐藏、子控件自身仍为 Visible 的组合需要额外有效可见性判断。Segmented 的 OnAnimate 在 slide 未运行时直接返回，运行时 `more || Control::OnAnimate(dt)` 又会短路基类；类似覆盖模式应系统审查。Drawer 已有终帧 Relayout 补修，证明终帧是实际踩坑点。

**优化：** 明确有效可见、实际在视口、窗口隐藏三种状态；基类动画每次该执行时必须执行，不依赖短路表达式。离屏暂停或 Snap 按控件类型定义，重现时无补播风暴。布局动画最终一帧必须刷新布局/脏区；仅透明度/平移尽量避免全根重新测量。

**验收：** 展开中折回、抽屉未关完重开、Tab 连切、动画子级藏进父容器、动画中删除控件；终态精确，空闲无持续帧请求，输入命中与视觉位置一致。

### R28 · P2：增加工程页的紧凑视觉规格，减少层层亮卡

**依据：** [ImeSettingsPage.cpp](../SHOWBOX-LUMEN/src/Ui/ImeSettingsPage.cpp) 同时使用外层 Lumen 卡和内部显式 Spotlight SettingsCard。对设置页而言，多层光点与留白是否干扰阅读，需要按完整窗口而非单控件判断。SettingsCard 当前还无条件绘制左侧 accent 竖条及 token 辉光，应明确它与“默认不发光”的规范关系。

**优化：** 在已有 Density/ThemeOverride 上补工程页规格：输入与按钮基线、行高、组标题、帮助文案、分隔与操作区统一。默认普通分组用平面或 Subtle，仅焦点区域显式 Spotlight；聚光卡交互子级继续通过 AvoidControls 隔离。主按钮一页只突出当前主要动作，风险操作用字形与文字表达，不加彩色主题。

**验收：** 打印、钢筋、设置三种完整页分别对比；同屏信息量提高，长时间阅读不靠鼠标移动才看清层级；GlowIntensity=0/中/高均清楚。

### R29 · P2：页面切换保留用户工作位置，并明确草稿状态

**依据：** [PageHost](./src/controls/page_host.cpp) 已有页面入退场；SHOWBOX 多页有保存/取消、抽屉编辑、隐藏后重新显示。动画系统与“这页是否已加载/是否有未保存修改”目前不是同一协议。

**优化：** 先提供组合样例：草稿、已保存快照、dirty、saving、failed，页面切换策略决定保留/丢弃/询问。按页面保存滚动位置和最后焦点；首次构建可懒加载，重回页面不反复播放大型入场。状态模型保持轻量，不引入必须使用的全局路由框架。

**验收：** 设置改一半切页再返回、抽屉取消、保存失败、隐藏重开；草稿语义一致，焦点不会落在不可见旧页，定位不丢失。

### R30 · P1：键盘优先级覆盖 IME、编辑器、浮层与宿主

**依据：** [WindowHost.cpp](../SHOWBOX-LUMEN/src/Ui/WindowHost.cpp:463) 存在宿主焦点被抢后的 Esc 热键处理，库本身还有 BindShortcut、输入控件 OnKey 与浮层 Esc。各层都“支持 Esc”不等于组合后行为正确。

**优化：** 明确处理顺序：IME 组合→当前编辑事务→最上层浮层→页面/窗口→宿主策略。提供默认动作/取消动作的作用域绑定；对话框形成焦点范围，关闭恢复到仍存活的触发器。触发器已销毁则选合理后备焦点。状态用焦点环和读屏信息表达，避免所有控件抢 Enter。

**验收：** 拼音选字 Enter 不提交整个表单；Esc 先取消编辑/弹层；Tab/Shift+Tab 无陷阱；返回 CAD 后快捷键只由正确宿主处理。此项需真实 IME/AutoCAD 人工验收。

## 7. 复合控件与用户操作流程

### R31 · P1：长期任务显示阶段、结果和取消状态

**依据：** [PlotBatchPage](../SHOWBOX-LUMEN/src/Ui/PlotBatchPage.cpp) 同时管理识别忙碌遮罩、逐行进度和总进度；钢筋页 submitting_ 则限制重复提交。已有 BusyOverlay/ProgressBar/InfoBar/Toast 可组合，但缺少一致的任务示例。

**优化：** 统一 idle/running/cancel_requested/succeeded/failed 状态适配；忙碌按钮能保留尺寸、展示阶段与可用取消动作。短任务避免一闪而过的遮罩；长任务保留结果摘要、失败行和重试入口。持续错误用 InfoBar/字段错误，Toast 用短反馈；提交被接收与业务成功分别表达。

**验收：** 极短任务、失败、重复点击、取消后仍执行中的任务、隐藏重开；不会只关闭遮罩就假装工作已取消，失败信息不因 Toast 超时而消失。

### R32 · P2：命令面板做成可组合列表，而非每次搜索重建按钮树

**已确认：** [PalettePage::rebuildGroupList](../SHOWBOX-LUMEN/src/Ui/Palette/PalettePage.cpp:326) Clear 后重建 Expander/Row/Button；每次 TextChanged 都执行。当前负载是否卡顿未测，但频繁重建成本与展开/焦点保持负担已在代码中可见。

**优化：** 利用 ItemsModel、ListView/TreeView 的已有虚拟化，补可复用命令项模板：图标、标题、别名、说明、快捷键、可用状态；稳定命令 key 保存展开/选择。搜索支持明确的中文/别名匹配，组合输入结束后再进行必要过滤；大目录可合并连续输入。按需增加最近使用/收藏，命令执行仍由 SHOWBOX 负责。

**验收：** 100/1000/10000 项搜索，清空搜索恢复展开状态；上下键与 Enter 可运行，空结果可读屏；过滤不重建全部控件，不丢搜索框选区。

### R33 · P2：先补高频复合字段与图标入口，再扩充低频控件

**依据：** [PlotBatchPage](../SHOWBOX-LUMEN/src/Ui/PlotBatchPage.cpp) 反复组合目录候选+浏览按钮；多个设置页组合图层文本+拾取动作；[PalettePage](../SHOWBOX-LUMEN/src/Ui/Palette/PalettePage.cpp) 自建 CommandIcon；[IconView.h](./include/lumen/IconView.h) 主要提供 Glyph。

**优化：** 优先提供 FieldWithAction/PathField、紧凑键值设置行、可折叠设置组等组合模板；从 Painter::DrawIconPath 提炼可缓存的 Path 图标入口。底层处理布局、禁用、焦点、帮助文本和图标尺寸。图层枚举、CAD 拾取、ACI/TrueColor/ByLayer/ByBlock 含义继续在应用层；库支持展示内容色，不把内容色变成主题 accent。

**验收：** 目录、图层、颜色三类字段分别迁移一个页面；能组合异步动作、取消和失败提示，业务不用碰控件内部或自己绘制标准边框。

### R34 · P1：UIA 操作应走与真实输入一致的业务提交路径

**已确认：** [NumberBox.h](./include/lumen/NumberBox.h) 的 AutomationSetRange 调用静默 Value；[ComboBox.h](./include/lumen/ComboBox.h) 的 AutomationCollapse 直接返回 true，未在该实现中关闭弹层。前者可能只改 UI 不更新业务绑定，后者报告成功却没有相应动作。

**优化：** 自动化操作统一进入控件内部的“用户操作/提交”通道，复用验证、事件和禁用判断；为 FormField 建立标签关联、错误文本关联，为表格逐格读写/行列语义补适合的能力。延续已修的 UIA 生命期门禁，不重写已稳定的 provider 清理流程。

**验收：** 用 UIA 改数字，页面模型同步改变；展开再折叠 Combo 实际收起；读屏可听到标签、值、错误、行列，元素销毁后返回不可用而非访问旧控件。

## 8. 渲染、性能、工程接入与文档

### R35 · P1：把“绘制路径零堆”从约定变成可验证边界

**已确认：** [Table::Draw](./src/controls/table.cpp:1810) 内存在首次 `make_unique<DrawCache>` 和缓存失效后的 CreateCommandList；footer 绘制调用 FooterText，后者遍历数据并构造字符串。即使只在首次/失效帧发生，严格的“绘制路径零堆”约定也尚未被完全满足。可见行绘制不等于聚合计算也只访问可见行。

**优化：** 缓存对象、图形资源与可见区文本准备放在非 Draw 阶段，布局/数据变化驱动资源更新；汇总值随模型更新计算并缓存。Draw 只消费准备结果。区分首次建树、数据准备、绘制、呈现的分配统计；不要用缓存命中帧的快速度掩盖滚动或悬停失效帧。回调填充 wstring 也须预留容量/复用并实测。

**验收：** 预热后静态帧、滚动帧、hover 变化、表尾聚合、长文本切换分别统计分配；Draw 范围为 0，聚合不在每次 hover 时 O(全表行数) 重算。

### R36 · P1：把历史表格绘制故障固化为交互序列回归

**依据：** [SHOWBOX 修复记录](../SHOWBOX-LUMEN/优化.md) 描述了文字随鼠标出现裁切、末列空白、首开尺寸等问题；当前 [visual 测试](./tests/visual/main.cpp) 已增加对应覆盖，不能声称没有测试。

**优化：** 在已有案例上补“同一个窗口连续操作”的图像和状态断言：鼠标跨单元格→列拖宽→冻结→横滚→改变字体/DPI→隐藏重开。核对 LumaText/DirectWrite、全帧/脏区、缓存命中/失效的同场景结果。缓存 key 包含实际依赖的几何、文本、字体、主题与设备状态，数据变更有明确失效通道。

**验收：** 静态文本区域不随 hover 莫名改变，最后一行/列完整或可滚到；非整数 DPI 无残影、旧裁剪和黑边；设备重建后不复用旧设备资源。遵守现有交换链、Present/Commit 顺序约束。

### R37 · P1：性能基准必须包含 SHOWBOX 的真实交互负载

**已确认：** [perf 测试](./tests/perf/main.cpp) 主要为 1280×800、300 帧离屏绘制；README、AGENTS 和历史修复记录中的数值不同。它可约束该场景，却不能证明 AutoCAD 内首开、编辑、布局和 Present 的延迟。

**优化：** 保留 <8 ms/帧预算，增加钢筋多列表、打印增量进度、命令过滤、设置页展开四类工作负载。拆分首开/预热、测量排列、Draw、Present 等待、输入到下一帧；报告 p50/p95/p99、最差帧、分配量、资源数量和空闲帧请求。记录 CPU/GPU、DPI、Release 配置、文本后端与数据规模后再谈性能提升。

**验收：** 基准同时记录新旧数据；更新/滚动压力下没有长尾卡顿，静止页面无持续动画唤醒；宿主资源循环至少观察窗口、USER/GDI、线程和 Private Bytes 趋势，不能只用工作集判断泄漏。0.17 ms 等旧成绩标为历史场景值。

### R38 · P1：CMake 消费接口不应迫使宿主清空编译选项

**已确认：** [SHOWBOX CMakeLists](../SHOWBOX-LUMEN/CMakeLists.txt:264) 为避免 `/permissive-` 干扰 ObjectARX/MFC，直接清空 lumen 的 INTERFACE_COMPILE_OPTIONS；另行读取 LUMEN_LUMATEXT_RUNTIME 拷贝 DLL。库已有 [lumen_copy_runtime](./CMakeLists.txt:124)，应核实为何应用没有直接使用，避免重复实现。

**优化：** 区分编译库自身的严格选项与消费者必须满足的接口要求；对确有需要的传播选项提供最小、可配置方式，保持库自身 /W4 零警告。运行时复制统一覆盖 exe/DLL/ARX，清楚说明 LumaText 源码模式、SDK 模式、许可证和缺 DLL 回退；不要让应用直接改目标内部属性。

**验收：** 独立 exe、MFC/ARX、add_subdirectory、安装 SDK 各有最小消费工程；Debug/Release 头文件变更能真正重编，中文依赖前缀问题不复发；应用无需清空整组 INTERFACE 选项。

### R39 · P1：API 文档写清行为契约，并用可编译用例保护

**已确认：** [NumberBox.h](./include/lumen/NumberBox.h) 顶部 Layout 写成了“顶层窗口”；一些头的 Keys 是泛化文案。公共属性、动作、事件又存在返回类型/通知行为差异。已有命名检查和 README 编译检查，但不能验证语义。

**优化：** 每个控件文档至少写：默认/最小尺寸，是否随 Density，值赋值是否通知，事件参数含义，草稿/提交语义，线程与模型生命期，键盘行为，空值/错误/隐藏状态。类型化绑定、Window 动作不强求同一种返回类型，但必须一致且可预测。新增每次踩坑的“错误用法→推荐用法→原因”短例，不只列能力名称。

**验收：** 新页面作者可仅凭公共头和样例完成数字表单/表格/Popup；API 编译用例覆盖真实链式顺序和 NumberBox 等派生控件，行为用例覆盖事件来源，修改时同步更新技能参考。

### R40 · P1：Gallery 增加真实业务场景，并以三个迁移闭环验收

**依据：** 现有 [Gallery](./examples/gallery/main.cpp) 按控件分类展示，适合发现能力；真实坑更多发生在混合布局、绑定、弹层和宿主焦点组合时。

**优化：** 增加不依赖 AutoCAD SDK 的模拟业务场景：工程数值表单、14 列钢筋选配、批量作业表、320 DIP 命令面板；提供长中文、空数据、错误、忙碌、数据突变和低动态效果切换。DumpTree 扩展约束/测量值/最终值、裁剪、焦点路径、布局触发原因与有效可见性；可选交互式布局边框调试。

**验收：** 每个阶段选择 SHOWBOX 的一个页面迁移并删除原有绕路代码，记录减少的尺寸副本、同步标志、subclass 或重复状态；库测试通过且页面交互保持，才能把优化标为完成。只新增 API、没有应用落地，不算完成。

## 9. 建议实施批次与依赖

| 批次 | 范围 | 先决条件 | 可交付结果 |
| --- | --- | --- | --- |
| A：稳定底座 | R01～R05 | 当前工作区冻结为可回溯基线 | 安全任务、连接、模型生命期；官方宿主样例 |
| B：输入与布局 | R08～R17 | R03，事件契约先定 | SettingsCard/TextBox 修复；可靠表单/数字提交；减少手动 sync |
| C：工程表格 | R18～R24、R35～R36 | R04、R11、R13～R15 | 层高表有类型编辑；钢筋表无需复制尺寸；打印增量刷新 |
| D：窗口与交互 | R06～R07、R25～R31、R34 | R01～R03、R17 | 通用 Popup、可预测焦点、统一动画和任务状态 |
| E：使用体验与交付 | R32～R33、R37～R40 | 随前四批滚动落地 | 命令面板/复合字段、性能报告、消费工程和完整页面样例 |

首个修复包建议限制在 R03、R09、R12、R14 的明确路径，以及 R25/R26 的 Segmented/Drawer 接入。R01/R02 先定关闭与错误协议，再实现，不能只给 detached worker 加一个 try/catch 就宣布安全。各包必须能单独评审和回退，不能把 40 项堆成一次大重构。

### 三个应用落地闭环

1. **RebarDesignPage：** 保持业务选配算法，用库尺寸查询替代 32/10/96 副本；统一选区同步；保留正确绘制；首开、缩放、14 列与列显隐通过。
2. **FloorHeightTablePage：** 用 Number/Enum 编辑器与提交事务替代分散文本解析；联动计算仍由页面执行；非法草稿可修复、取消不写回。
3. **PalettePage + AcadColorPicker：** 命令过滤保留树状态；焦点监听不再派生 SearchBox；颜色弹窗用通用 Popup；宿主焦点与 CAD 颜色语义留在 SHOWBOX。

PlotBatchPage 作为贯穿批次 C～E 的性能/状态压力页，重点测逐行进度，不要求在首批同时重构完整打印流程。

## 10. 验收矩阵与当前未验证项

以下是后续实施要求，本次文档审查未执行这些运行测试。

| 层级 | 必须覆盖 | 判定 |
| --- | --- | --- |
| 构建/API | `build.bat`、API 链式、命名、README 用例 | C++20，/W4 零警告；正确 vcvars 环境，避免旧对象混链 |
| 视觉 | 原 visual 全套 + 设置卡/表格/字体/DPI/减少动态效果 | ALL PASS；历史像素故障仍受保护 |
| 动画 | 原 anim + 中断、终帧、父隐藏、motion=0 | ALL PASS；到位后停止，基类状态同步 |
| 数据/输入 | 程序赋值、用户输入、绑定、UIA、非法草稿、排序/过滤 | 模型与显示一致，无误提交和错行编辑 |
| 性能 | 原 perf + 四类真实工作负载 | 原预算 <8 ms/帧；报告长尾/分配/空闲，不只平均值 |
| 生命期 | 源先销毁、窗口先销毁、投递失败、异步异常、重复重启 | 无悬空回调；失败保留可重试状态 |
| 独立实机 | Gallery 悬停、聚光、按压、Tab、DPI、缩放 | 实际窗口检验，不以启动成功替代 |
| AutoCAD | 加载卸载、多窗模态、停靠、跨屏、文档切换、命令中操作 | 在目标宿主验证，应用/库各自资源归属清楚 |
| 人工专项 | 真实鼠标追光手感、中文 IME 候选、读屏、触摸板 | 明确记录待确认结果与设备环境 |

## 11. 实施约束

- 保持纯黑单色、accent 纯白；语义靠亮度与字形，CAD 内容色不扩展成彩色主题。
- 光效继续走 Theme token 与 GlowIntensity；聚光位置当帧跟随，不能用 Spring/Tween 平滑鼠标坐标。
- 普通控件默认不自开聚光，交互子控件按现有 AvoidControls 规则阻挡父卡光斑。
- 公共 API 只在 include/lumen，系统类型保留在内部；新增通用接口不引入 ObjectARX 依赖。
- 控件仍一控件一编译单元，跨控件共用逻辑进入恰当核心服务/接口，避免扩散反向依赖。
- 动画跟 Present 驱动；业务延迟/合并更新可用定时能力，不用 WM_TIMER 替代动画时钟。
- 新控件使用 ControlOf/PanelOf；事件成对提供 On/Bind，属性不恢复 Set/Get 双轨。
- 渲染、文本与设备链修复遵守现有 PREMULTIPLIED、FLIP_SEQUENTIAL、ResizeBuffers 资源释放及 Present 先于 Commit 约束。
- 已有 WeakRef、Property、ItemsModel、Form、Popup 内部通道和文本服务优先复用；补齐能力，不建立第二套平行框架。
- 本文所列 P0/P1 需要先完成可复现用例再修，所有“改善手感”“更清晰”结论必须有完整页面对比与实机依据。

## 12. 本次交付记录

- 完成两仓库当前源码交叉审查，覆盖宿主/线程、典型业务页、窗口/输入/模型/表格/动画/渲染、构建与现有测试。
- 形成 40 项优化条目，区分当前代码问题、实战能力缺口、设计建议与历史已修回归。
- 写入本文件并检查路径、编号、Markdown 链接目标、UTF-8 编码与差异；仅文档交付，不改变现有代码行为。
- 未声称本次完成任何库修复或运行验收；AutoCAD/IME/真实鼠标与视觉对比保留为后续实施验证。
