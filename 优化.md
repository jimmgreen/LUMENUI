# LUMEN 高级化优化建议

基于当前仓库现状（73 个控件、单暗色单色主题、D2D 立即模式全帧重绘、0.17 ms/帧、可选 LumaText、`window_impl.cpp` 约 2900 行）给出的建议。"高级"分两个层面：**用户看得见的质感**（视觉、动效、手感、无障碍）和**开发者感受到的成熟度**（API 表达力、架构、工程质量）。两者缺一都会显得"业余"。

所有建议均遵守 `AGENTS.md` 既定约束：纯黑单色、accent 恒白、光感走 token、绘制路径零堆分配、动画只在需要时运行、不引入亮色主题。

---

## 一、视觉质感（最直接拉开差距的部分）

### 1.1 消灭色带（banding）—— 暗色 UI 的头号"廉价感"来源

现状：交换链与离屏目标都是 `DXGI_FORMAT_B8G8R8A8_UNORM`（8 bit），而 LUMEN 到处是 alpha 0.03～0.10 的大半径径向渐变（600 px 光斑、顶部环境辉光、Glow）。8 bit 下纯黑到 3% 白之间只有约 8 个灰阶，肉眼一定能看到同心环。

建议（按性价比排序）：
- **抖动噪声叠层**：在 `Painter` 里常驻一张 64×64 的蓝噪声/三角噪声位图（BGRA，alpha ≈ 1/255～2/255），用 `ID2D1BitmapBrush` 平铺（`D2D1_EXTEND_MODE_WRAP`）在 `DrawBackdrop`、`DrawSpotlight`、`DrawGlow` 之后叠一遍。成本一次 FillRect，效果是渐变彻底平滑。这是电影/游戏 UI 的标准做法。
- **渐变停靠点加密**：`RadialBrush` 目前是 inner→outer 两停靠（`inner_stop` 一个中间档）。改为 4～5 个停靠点近似 `smoothstep`/gamma 曲线，线性插值造成的"中段过亮"会明显减轻。
- **（可选）16 bit 浮点后台缓冲**：`DXGI_FORMAT_R16G16B16A16_FLOAT` 组合交换链 + DComp 是允许的，但显存翻倍且需要验证 LumaText 桥的位图格式。仅在前两项不够时考虑。

验收：visual test 新增"渐变平滑度"断言——沿聚光卡中心向外采样 64 点，相邻亮度差的最大值不得超过阈值（有抖动后是噪声而非台阶）。

### 1.2 真实背景模糊（Acrylic 质感）

现状：Dialog/Drawer/BusyOverlay 的遮罩是 80% 黑纯色，README 路线图里"模态遮罩真高斯模糊"仍未做。

建议：
- 覆盖层出现时，把当前场景（DrawTree 结果）`CopyFromRenderTarget` 到一张中间位图，用 `CLSID_D2D1GaussianBlur`（sigma 12～20，`D2D1_GAUSSIANBLUR_OPTIMIZATION_SPEED`）+ `CLSID_D2D1Saturation`（拉低）做一次效果图，缓存为 `blurred_backdrop_`。
- **只在下层内容变化时重算**（覆盖层期间下层通常静止），常态帧直接 `DrawImage` 缓存位图 —— 和现有 `backdrop_cache_` 同一套脏标记模式，不违反"绘制路径零堆"。
- 覆盖层入场 240 ms 内模糊半径从 0 → 目标做 Tween（效果属性 `SetValue(D2D1_GAUSSIANBLUR_PROP_STANDARD_DEVIATION)` 每帧突变，不重建）。
- 在模糊层上再叠 1.1 的噪声，Acrylic 的"磨砂"感来自噪声，不只是模糊。

### 1.3 层级/海拔系统（Elevation tokens）

现状：Glow 的 spread、Flyout 的 specular line、卡片描边各控件自己拼。

建议在 `Theme` 里增加 `Elevation` 枚举（0 Flat / 1 Raised / 2 Overlay / 3 Modal）与对应 token 组（glow spread、glow alpha、边缘折射强度、顶部镜面线 alpha），`Painter::DrawElevated(rect, radius, Elevation)` 一次画齐。Menu/Flyout/ToolTip/Dialog/Toast 统一走它，保证"越浮越亮"的层级感在所有浮层上一致。

### 1.4 排版精修

- **数字用表格字形**：Table/StatusBar/NumberBox/Gauge 的数字应开 `DWRITE_FONT_FEATURE_TAG_TABULAR_FIGURES`（`IDWriteTypography`），否则列对不齐、计数跳动。
- **大写小标题加字距**：Caption/Overline 类（导航分组标题、Badge 文字）加 `+0.06em` letter-spacing（`IDWriteTextLayout1::SetCharacterSpacing`），这是"设计系统级"的细节。
- **新增 TextRole**：`Overline`（11 px、加字距、次要色）、`Subtitle`、`Numeric`（表格字形）。现在 8 个 role 里 `Display` 与 `Title` 之间断档。
- **CJK 回退链固定**：`ResolveFamily` 现在只有 body/icon 两个家族。CJK 应显式回退到 `Microsoft YaHei UI`，并统一基线（`DWRITE_PARAGRAPH_ALIGNMENT` 无法救基线，需要按 fallback 家族的 ascent 校正 y 偏移），否则中英混排行内会上下跳。
- **文字反锯齿策略**：`renderer.cpp:89` 对 premultiplied 半透明合成表面用 ClearType。ClearType 依赖不透明底，在半透明卡片/Glow 上会出彩边。建议全局 `GRAYSCALE` + 调 gamma（`IDWriteRenderingParams` 自定义 gamma 1.8～2.0、enhanced contrast 0.5），暗底白字用灰度 AA 反而更干净。

### 1.5 光感的进一步"讲究"

- **聚光边缘折射与内部光斑分离视差**：边缘光环中心相对鼠标沿卡片中心方向偏移 5～8%（纯几何计算，不违反"位置不走时钟"），玻璃折射的错觉立刻出来。
- **溢光（spill）**：聚光卡的光斑轻微照亮相邻卡片的描边（窗口层做一次 pass：对所有开启聚光的卡片，用同一个 `mouse` 位置给邻近卡片描边叠加低 alpha 径向）。Bento 网格上会像一盏真灯。
- **按压光爆**：Button 按下瞬间从按压点起一个 0.35 s 的径向白光扩散（`Tween` + `FillRoundedRectRadial`，仅按压期间动画），替代或叠加现有中心收缩。
- **焦点环生长动画**：焦点环从 spread 0 → 2 px、alpha 0 → 1 用 120 ms `CssEaseOut`，而不是瞬时出现。

---

## 二、动效与手感

### 2.1 Motion token 统一

现状：各控件的 `EaseTo` speed（12、18…）、Tween 时长散落在实现里。

建议 `Theme` 增加：
```cpp
float duration_fast = 0.12f;    // 状态切换（hover、check）
float duration_normal = 0.24f;  // 出现/消失、展开
float duration_slow = 0.40f;    // 页面切换、抽屉
Ease  ease_standard = Ease::Material;
Ease  ease_enter = Ease::CssEaseOut;
Ease  ease_exit = Ease::CssEaseIn;
float motion_scale = 1.0f;      // 0 = 系统"减少动画"
```
所有控件从 token 取值。**顺带实现"减少动画"**：`SystemParametersInfo(SPI_GETCLIENTAREAANIMATION)` 为假时 `motion_scale = 0`，Shimmer/呼吸/骨架扫光全部静止，聚光只保留跟随不做渐显。这是无障碍要求，也是"专业库"的标志。

### 2.2 编排（Choreography）

- **列表/网格入场级联**：ListView/GridView/Bento 卡片首次出现时按 index 错开 20 ms 淡入 + 8 px 上移。只在首帧/数据源替换时播，滚动不播。
- **NavigationView 页面切换**：当前只有回调，页面替换是应用自己做的瞬切。提供 `NavigationView::SetPage(std::unique_ptr<Control>)` 或 `PageHost` 容器：旧页 120 ms 淡出下沉 8 px、新页 200 ms 淡入上浮，方向随导航项索引变化（向下选→内容从下进）。缓存已访问页面避免重建。
- **Expander/SettingsCard 展开**：高度用 `Tween` 而不是瞬时，内容 `Clip(true)` 裁切。
- **Dialog**：卡片 `scale 0.96→1 + alpha`（`PushScale` 已有），退出反向 + 更短。
- **Toast**：入场已有；增加堆叠时其余条目的位移 Tween（现在是否会跳位需实机确认）。

### 2.3 滚动手感

现状：ScrollViewer 无惯性/弹簧，滚轮是步进。

- 滚轮目标位置用 `SpringMotion`（`Spring::Smooth`）趋近，连续滚轮累加目标；触摸板高精度滚轮（`WM_MOUSEHWHEEL`/小 delta）直接跟手不加弹簧。
- 到边界的**过卷回弹**（overscroll）：允许超出 0～max 一段（阻尼 0.3），松手后 `Spring::Snappy` 回位。这是 macOS/移动端的"高级"信号，Windows 原生极少见。
- 覆盖滚动条的**自动隐藏**：无滚动 1 s 后淡出（alpha Tween），悬停轨道时加粗（`expand` 已有）。

### 2.4 触摸与笔

现状：只处理鼠标消息（无 `WM_POINTER`/`WM_TOUCH`）。

- 切到 `WM_POINTER*` 统一处理鼠标/触摸/笔，触摸时命中测试加 8 px 容差（hit slop）、ListView/ScrollViewer 支持拖动平移 + 惯性（`SpringMotion` 速度衰减）。
- Slider/RangeSlider 触摸时拇指放大。

---

## 三、无障碍与键盘（真正的"成熟度"分水岭）

### 3.1 UI Automation 提供程序

现状：`Control::SetAccessibleName` 只存字符串，没有 provider，屏幕阅读器/自动化测试工具看到的是一个空窗口。

建议分阶段：
1. 窗口根 `IRawElementProviderSimple + IRawElementProviderFragmentRoot`，`WM_GETOBJECT` 返回。控件树 → UIA 树（`Navigate`、`GetRuntimeId`、`get_BoundingRectangle` 来自 `AbsoluteBounds`）。
2. 常用模式：Button → `IInvokeProvider`；CheckBox/Switch/ToggleButton → `IToggleProvider`；TextBox/Slider/NumberBox → `IValueProvider`/`IRangeValueProvider`；ListView/Table/ComboBox → `ISelectionProvider`+`ISelectionItemProvider`；Expander → `IExpandCollapseProvider`；TabControl/NavigationView → `ISelectionProvider`。
3. 事件：焦点变化 `UiaRaiseAutomationEvent(UIA_AutomationFocusChangedEventId)`，Toast/InfoBar 用 `LiveSetting = Assertive` 让读屏播报。
4. Control 基类加 `virtual ControlType AutomationType()`、`virtual AutomationPattern* Pattern(id)`，实现在 `src/core/uia.cpp`，不污染控件实现文件。

副产品：可以用 Inspect.exe / UIA 客户端写**交互自动化测试**，而不只是像素快照。

### 3.2 焦点可见性（focus-visible）

现状：没有键盘/鼠标输入来源区分，鼠标点按钮也可能画焦点环。

建议 WindowImpl 记录 `last_input_was_keyboard_`（`WM_KEYDOWN` 置真、`WM_LBUTTONDOWN`/`WM_POINTERDOWN` 置假），`Control` 提供 `FocusVisible()`，只有键盘导航时画焦点环。这是 CSS `:focus-visible` 的语义，所有现代 UI 都这么做。

### 3.3 键盘导航补全

- **方向键漫游（roving tabindex）**：RadioButton 组、Segmented、CommandBar、TabControl、NavigationView 内部用方向键移动，Tab 只进出整组一次。现在 Tab 逐个控件走会很啰嗦。
- **助记键**：MenuBar `&文件` 下划线 + Alt 激活；Button 文本 `&确定`。
- **F6 区域轮转**、**Esc 逐层关闭**（Flyout → Drawer → Dialog）、Dialog 内 **Enter 默认钮 / Esc 取消钮**语义（`DialogSpec` 增 `default_button`）。
- Table/TreeTable 单元格级键盘导航（方向键 + F2 编辑 + Ctrl+C 复制 TSV）。

### 3.4 高对比度与系统文字缩放

- `SPI_GETHIGHCONTRAST` 为真时：关闭全部 Glow/聚光（`glow_intensity = 0`），描边 alpha 提到 1.0，保证 WCAG 对比度。单色体系天然接近，只差这一开关。
- 跟随系统"文本大小"（`Settings > 辅助功能 > 文本大小`，读 `SPI_GETLOGICALDPIOVERRIDE`/UISettings TextScaleFactor）：`Theme` 增加 `text_scale`，MeasureText 乘上去。

---

## 四、API 表达力（开发者眼里的"高级"）

### 4.1 多订阅事件与连接句柄

现状：每个事件是单个 `std::function`，`OnClick` 第二次调用会覆盖第一次；无法解绑。

建议 `lumen::Signal<Args...>`：
```cpp
lumen::Connection c = button.OnClick([]{ ... });   // 可多订阅
c.Disconnect();                                    // 或 ScopedConnection RAII
```
实现用小向量 + 递增 id，触发时拷贝到栈上的固定缓冲避免迭代中修改。控件实现里 `click_()` 改成 `click_.Emit()`，公共签名不变（返回值从 `Button&` 变为可隐式转为 `Button&` 的代理，或者提供 `OnClickScoped` 新接口）。

### 4.2 响应式属性与绑定

现状：无数据绑定，界面同步全靠手写回调。

建议最小可用的 `Property<T>`：
```cpp
lumen::Property<int> count{0};
label.BindText(count, [](int n){ return std::format(L"{} 项", n); });   // 单向
slider.BindValue(volume);                                                // 双向
count = 5;   // 自动 Invalidate/RelayoutParent
```
- 订阅列表按 4.1 的 Signal；控件析构自动断开（`ScopedConnection` 成员）。
- 不做表达式解析、不做反射，保持 C++ 零成本气质。
- `Computed<T>` 派生属性可选。

### 4.3 声明式构建

现在的 `Add<T>(...)` 链式风格已经不错，缺"结构一眼看清"的嵌套写法：
```cpp
window.Root().Children(
    Column().Comfortable().Children(
        FormField(L"名称").Child(TextBox().Placeholder(L"项目名")),
        Row().Children(Button(L"取消"), Button(L"确定", ButtonKind::Primary).Ref(ok))
    )
);
```
实现：`Children(Args&&...)` 接受右值控件对象并 `std::make_unique<T>(std::move(...))`（控件需要可移动构造，或用 `Add(std::unique_ptr<Control>)` 底层）。`.Ref(T*&)` 拿回引用。与现有 API 共存。

### 4.4 Command 对象

MenuBar / CommandBar / Button / 快捷键现在各自绑回调，`SetEnabled` 要手动同步。

```cpp
lumen::Command save{L"保存", icon::kSave, L"Ctrl+S", [&]{ ... }};
save.CanExecute([&]{ return doc.dirty; });     // 变化时所有绑定处自动启用/禁用
toolbar.Add(save); menu.Add(save); window.Bind(save);
```

### 4.5 布局补全

现状缺：`MinWidth/MaxWidth/MinHeight/MaxHeight`、`Margin`、层叠容器、Grid 跨行跨列/显式行、`AspectRatio`。

- `Control::MinSize/MaxSize/Margin`：Measure 阶段钳制，Arrange 阶段扣除。这是最常被问到的缺口。
- `ZStack`（层叠面板）：子级按 `Anchor`（9 宫格）叠放，用于图片上的角标、卡片右上角关闭钮、加载遮罩。现在做这类需求只能 `SetBounds`。
- `Grid`：`Rows(...)`、`Span(col, row)`、`Place(child, col, row)`，否则复杂表单还得多层嵌套。
- `Dock`（上下左右 + fill）对传统桌面布局很顺手。
- `Viewbox` 已有；补 `AspectRatio(16/9)` 容器。

### 4.6 数据模型接口

ListView/Table/TreeView 都是"回调拿文本"的虚拟化模型，够快但增删改要手动 `SetItemCount + Invalidate`。

建议 `ItemsModel` 抽象（`Count()`、`Get(i, Row&)`、变更信号 `Inserted/Removed/Changed/Reset`），控件订阅后自动局部刷新并做插入/删除动画（行高 Tween）。提供 `VectorModel<T>` 与 `FilteredModel/SortedModel` 装饰器，Table 排序的"视图行/数据行双空间"也能借此收敛到一个地方。

### 4.7 主题局部覆盖与密度

- `Control::Style(ThemeOverride)`：允许单个子树覆盖半径/间距/字号（如紧凑工具栏），`Draw` 时取 `EffectiveTheme()`。实现为父链向上查找的轻量 `const Theme*`，无覆盖时零成本。
- `Theme::density`（Comfortable/Normal/Compact）一次性缩放 `button_height/input_height/list_row_height/space_*`。

---

## 五、渲染架构

### 5.1 脏矩形部分重绘

现状：任何 `Invalidate` 都全窗重绘（0.17 ms 很快，但笔记本上鼠标划过聚光卡就是持续 60/120 fps 全屏 GPU 提交，功耗与风扇会说话）。

- `Control::Invalidate()` 携带矩形（`absolute_` 外扩 glow spread，聚光卡外扩 600 px 光斑半径或直接整卡）；窗口合并为脏矩形列表（≤ 8 个，超出则合并为包围盒）。
- `Present1` 传 `DXGI_PRESENT_PARAMETERS::pDirtyRects`，D2D 侧 `PushAxisAlignedClip` 到脏区并只绘制与其相交的子树（`DrawControlTree(..., clip)` 已存在）。
- FLIP_DISCARD 下必须保留上一帧内容才能部分更新：改用 `FLIP_SEQUENTIAL` 或在 D2D 侧从上一帧位图复制（`ID2D1Bitmap1::CopyFromBitmap`）。
- Backdrop 缓存已有，是这条路的基础。

### 5.2 静态子树缓存（命令列表）

Table/ListView 滚动时每帧重画全部可见行。对已布局且未失效的子树录制到 `ID2D1CommandList`，滚动只是变换偏移 + 重录新进入的行。文本 layout 已缓存，所以主要收益是几何和画刷调用数。

### 5.3 多显示器/高刷

- 120/144 Hz 下 `EaseTo(speed)` 是帧率无关的（用 dt），Tween 也是；确认所有 `OnAnimate` 没有按帧计数的地方（如 shimmer 相位）。
- 跨显示器 DPI 切换时重建 `PrepareRoundedClip` 等几何缓存（现在有没有做需要检查 `WM_DPICHANGED` 路径）。

### 5.4 公共头卫生

`Painter.h` 直接 `#include <windows.h>` 和 `<d2d1_3.h>`，与"公共头不暴露系统类型"自相矛盾（虽然规范给了例外）。把 D2D 相关（`BeginFrame(ID2D1DeviceContext2*)`、`DrawBitmap(ID2D1Bitmap*)`、`CreateBitmapBgra`）挪到 `lumen/PainterNative.h`，主头改为 `class PainterImpl*` PIMPL。用户项目 include 一个控件头就拉进整个 `windows.h` 是编译时间和宏污染（`min/max`、`GetObject`）的常见投诉点。

---

## 六、控件深度（做精十个，胜过做多七十个）

优先打磨使用频率最高的核心控件，每个都对照 WinUI 3 / Fluent 2 的行为清单逐项补齐：

| 控件 | 现状缺口 |
|---|---|
| TextBox | 双击选词/三击选行、Ctrl+Backspace 删词、拖放文本、撤销按词分组、浮动标签（placeholder 上浮成 label）、右键标准菜单、`MaxLength`、输入掩码 |
| Table | 冻结列、列拖动重排、列显隐菜单、分组 + 粘性组头、页脚聚合、列虚拟化、键盘单元格导航、多列排序 |
| ListView | 拖动重排、滑动操作、分组头、增删动画（依赖 4.6）、空态内嵌 EmptyState |
| ComboBox | 大量项虚拟化（路线图项）、分组、多选 + Chip 展示、键入跳转 |
| Menu | 图标列、勾选/单选项、助记键、分组标题、子菜单延迟与"三角安全区"（斜向移入子菜单不误关） |
| Dialog | 任意内容对话框（不只 title+message）、尺寸档、默认钮/取消钮语义、结果 `std::future`/回调 |
| TabControl | 可关闭标签、拖动排序、溢出滚动/下拉、中键关闭、拖出成新窗（远期） |
| NavigationView | 多级项（可展开）、内置搜索框、底部固定项、`PageHost` 切换动画、面包屑联动 |
| ScrollViewer | 惯性/回弹（2.3）、锚点滚动（内容变化保持视口）、`ScrollIntoView` 带动画 |
| Chart | 悬停十字线 + 数据提示、图例交互（点击隐藏系列）、数据变化时的补间、缩放/平移 |

---

## 七、工程质量

### 7.1 拆分 `WindowImpl`

2900 行一个类，同时管 Win32 消息、渲染、输入路由、焦点、Toast、ToolTip、Flyout、Dialog、Drawer、Busy、定时器、托盘、IME、OLE 拖放、快捷键。建议按职责拆：
- `InputRouter`（命中测试、捕获、悬停、聚光同步、焦点遍历）
- `OverlayHost`（Toast/ToolTip/Flyout/TeachingTip/Dialog/Drawer/Busy 的统一栈：谁在最上、Esc 给谁、焦点回哪）
- `FrameChrome`（Client 边框、标题栏命中、Snap Layouts、DWM 属性）
- `ImeBridge`、`TrayHost`、`TimerHost`
每个 200～500 行，`WindowImpl` 只做组合。

### 7.2 收缩 `Control` 基类体积

每个 `Control` 内嵌 `Menu context_menu_`、两个 `std::wstring`、一个 `std::function`（Panel 再加一个）、`unique_ptr<ToolTip>`。100 k 行虚拟列表不受影响（虚拟化），但 Bento/表单类界面上千个控件时内存与缓存友好度都吃亏。把冷数据（tooltip、context menu、accessible name）挪到 `std::unique_ptr<ColdData>` 按需分配。

### 7.3 测试体系

- **交互测试**：`offscreen.h` 已能渲染，补一个 `OffscreenWindow` 能注入鼠标/键盘事件（直接调 `WindowImpl::OnMouseMove/OnKeyDown`），断言状态与像素。现在悬停/焦点/键盘导航全靠人工。
- **布局单元测试**：Row/Column/Grid/WrapPanel 的 Measure/Arrange 用数值断言（Grow 分配、AlignMain、Padding、Min/Max），比 PNG 稳定。
- **金图对比**：visual test 改为与仓库内基线 PNG 做感知差异（每像素 ΔE 或 SSIM），任何控件的意外视觉回归都能抓到，不只是几个采样点。
- **CI**：GitHub Actions `windows-latest` 跑 `build.bat` + 三个测试 + `/analyze`；PR 附上 visual PNG 产物。
- Debug 配置开 ASan（MSVC 已支持）与 `/fsanitize=address`；周期性用 clang-cl 编一次抓可移植性警告。

### 7.4 发布形态

- vcpkg overlay port / CPM 片段；语义化版本 + `CHANGELOG.md`；`lumenConfig.cmake` 已有，补 `lumen::lumen` 的 `INTERFACE_COMPILE_FEATURES cxx_std_20`。
- 文档站：用 visual test 自动产出每个控件的状态板 PNG 插入文档，保证图文永不脱节。Gallery 每页加"查看源码"按钮（把该 section 的 cpp 文本拷到剪贴板或用系统编辑器打开）。
- 提供 2～3 个**完整示例应用**（设置中心、日志查看器、文件管理器），比控件画廊更能展示"这个库能做真产品"。

### 7.5 可观测性

- `log.h` 已有；补 `LUMEN_TRACE` 帧级事件（布局次数、DrawCall 数、画刷缓存命中率、文本布局缓存命中率），Gallery 的 perf HUD（已有 fps）显示这些数字，性能回归一眼可见。
- 设备丢失、字体缺失、LumaText 加载失败这类事件走结构化日志而不是静默回退。

---

## 八、建议的推进顺序

**第一阶段（1～2 周，立刻可感知）**
1. 抖动噪声 + 渐变停靠点加密（1.1）
2. Motion token + 减少动画（2.1）
3. focus-visible（3.2）
4. 滚动惯性 + 滚动条自动隐藏（2.3）
5. Min/Max/Margin + ZStack（4.5 前两项）
6. `Painter.h` 去 `windows.h`（5.4）

**第二阶段（3～6 周，结构性）**
7. 背景真模糊 + Elevation token（1.2、1.3）
8. Signal 多订阅 + Property 绑定（4.1、4.2）
9. NavigationView PageHost 切换动画 + 列表级联入场（2.2）
10. WindowImpl 拆分（7.1）+ 交互测试与布局单测（7.3）
11. 排版：表格数字、字距、CJK 基线、灰度 AA（1.4）

**第三阶段（长期，护城河）**
12. UIA 提供程序（3.1）
13. 脏矩形 + 命令列表缓存（5.1、5.2）
14. WM_POINTER 触摸/笔（2.4）
15. ItemsModel 数据层 + Table/ListView 深度补齐（4.6、六）
16. 声明式构建、Command、主题局部覆盖（4.3、4.4、4.7）

**明确不做（与设计语言冲突）**
- 亮色主题、彩色语义、系统强调色跟随
- 每帧堆分配的"便利"API、常驻动画
- Mica/云母等带色彩的系统材质（纯黑体系用自绘模糊即可）

---

## 九、每项的验收方式（与现有测试对齐）

- 视觉类：`lumen_visual_test` 增加对应像素/平滑度断言，PNG 入库做金图。
- 动效类：`lumen_anim_test` 增加曲线与时长断言；Gallery 实机检查记录为人工项。
- 性能类：`lumen_perf_test` 场景加入模糊覆盖层、级联入场、1000 卡片 Bento；预算仍 < 8 ms，并新增"空闲一秒内 Present 次数 = 0"断言防止动画空转回归。
- 无障碍类：用 `Inspect.exe`/UIA 客户端脚本验证控件树与模式；`SPI_GETCLIENTAREAANIMATION`/高对比度用注册表模拟后跑 visual test。
- API 类：`examples/template` 与 README 快速上手同步更新，任何新 API 必须在 Gallery 有对应 section。

---

## 十、易用性第二轮（开发者体验）

前九节里 API 相关的项（Signal、Property、Children/Ref、Command、ItemsModel、Min/Max/Margin、ZStack、Style/Density、PageHost、Post/Timer、Dispatch* 测试注入）已经落地。这一轮不再看"缺什么能力"，而是看**用现有能力写一个真实应用时哪里会卡手**。证据主要来自两处：公共头本身，以及 Gallery 为了绕过库限制而自己写的代码（`examples/gallery/common.h/.cpp`、`main.cpp`）——Gallery 需要"绕"的地方，用户也一定需要。

### 10.1 链式调用的三处断点

现状：
- `Control::SetVisible(bool)` 返回 `void`（`Control.h:70`），`SetSpotlight(bool)` 同（`Control.h:157`）。全库没有任何转发，链里一出现 `SetVisible` 就断，只能另起一行。
- `Margin/MinSize/MaxSize` 的派生类转发只有 Panel 家族和 ScrollViewer 有（`Panel.h`、`ScrollViewer.h`）。Button/Label/TextBox/ListView/Table/ComboBox 等全部叶子控件没有：`Add<Button>(L"OK").Margin(8.0f).OnClick(...)` 编不过，因为 `Margin` 返回 `Control&`。
- 55 个控件头各自复制粘贴 `SetToolTip/SetEnabled/Grow/FillCross` 转发（`grep "& SetToolTip(std::wstring_view text)"` 命中 55 个文件）。每新增一个基类 setter 都要改 55 处——这次 `Margin` 就是这样漏掉的。

建议：
```cpp
// lumen/ControlOf.h —— CRTP 中间层，基类 setter 一律返回 Derived&
template <class D> class ControlOf : public Control {
public:
    D& SetVisible(bool v)      { Control::SetVisible(v);  return Self(); }
    D& SetEnabled(bool v)      { Control::SetEnabled(v);  return Self(); }
    D& SetToolTip(std::wstring_view t) { Control::SetToolTip(t); return Self(); }
    D& Grow(float w = 1.0f)    { Control::Grow(w);        return Self(); }
    D& FillCross(bool v = true){ Control::FillCross(v);   return Self(); }
    D& Margin(float u)         { Control::Margin(u);      return Self(); }
    D& MinSize(Size s)         { Control::MinSize(s);     return Self(); }
    D& MaxSize(Size s)         { Control::MaxSize(s);     return Self(); }
    D& Style(const ThemeOverride& o) { Control::Style(o); return Self(); }
    D& Density(lumen::Density d)     { Control::Density(d); return Self(); }
    D& SetSpotlight(bool v)    { Control::SetSpotlight(v); return Self(); }
    D& SetContextMenu(Menu m)  { Control::SetContextMenu(std::move(m)); return Self(); }
private:
    D& Self() noexcept { return static_cast<D&>(*this); }
};
class Button : public ControlOf<Button> { /* 删掉全部转发样板 */ };
```
Panel 家族同理 `PanelOf<D>`（再包 `Clip/Background/Children/Padding`）。公共签名不变，删几百行样板，以后新增基类 setter 改一处。`Control::SetVisible` / `SetSpotlight` 本身也改为返回 `Control&`。

### 10.2 命名风格三套并存

现状（同一语义、不同前缀）：
- 无前缀重载对：`Label::Text()/Text(v)`、`Button::Kind()/Kind(v)`、`NumberBox::Value()/Value(double)`、`ColorSwatch::Selected(bool)`。
- `Set` 前缀：`Slider::SetValue(float)`、`ProgressBar::SetValue`、`Gauge::SetValue`、`CheckBox::SetChecked`、`Chip::SetSelected`、`ListView::SetSelectedIndex`。
- `Get/Set` 对：`Button::GetSize()/SetSize()`、`Window::GetToastMotion()/SetToastMotion()`。
- 一个类里两套：`SplitButton::Checked(bool)` 与 `SetChecked(bool)` 并存（`SplitButton.h:54/60`）。
- 事件注册返回值不对称：`Panel::OnClick` 返回 `void`（`Panel.h:72`），`Button::OnClick` 返回 `Button&`（`Button.h:52`）。
- `Button::GetSize()/SetSize(ButtonSize)`（`Button.h:41-42`）与几何类型 `lumen::Size` 撞名，读代码时 `btn.GetSize()` 会被当成尺寸。应为 `SizeClass()`。
- 对齐枚举三套：`Align { Leading, Center, Trailing }`（`Core.h:78`）、`StackPanel::CrossAlign { Stretch, Start, Center, End }`、`ZStack::Align { Start, Center, End, Stretch }`——同一概念三种拼写与顺序，Gallery 只好自己 `using Cross = StackPanel::CrossAlign`（`common.h:10-11`）。应提升为命名空间级 `CrossAlign/MainAlign`，嵌套枚举留别名。
- 间距命名分裂：StackPanel 叫 `Spacing`，Grid/WrapPanel 叫 `Gap`（`Panel.h:180/254/329`）。二选一，另一个做别名。

Gallery 自己就踩了这个坑：`common.cpp:171-176` 里 `g_intensity->SetValue(i * 100)` 与 `g_intensity_box->Value(i * 100)` 并排出现——两个都是"把数值写进输入控件"。

建议：只留一种，写进 `AGENTS.md`：
- 属性 = 无前缀重载对（`Value()` 读、`Value(x)` 写并返回 `Self&`），与 `Text/Title/Kind/Placeholder` 一致；
- "编程赋值不触发事件"的语义靠文档注释表达，不靠 `Set` 前缀区分；
- 动作（非属性）用动词：`ScrollTo`、`Dismiss`、`Focus`；
- 旧 `Set*`/`Get*` 保留一个版本并 `[[deprecated("use Value()")]]`，Gallery 与 README 全部换新写法。

### 10.3 事件：Signal 只落地了 5 个控件

现状：
- 用 `Signal` 的只有 Button / SplitButton / HyperlinkButton / SettingsCard / Panel。其余约 70 个 `On*` 仍是单槽 `std::function`，第二次注册静默覆盖第一次、无法断开：`TextBox.h:49`、`ListView.h:91`、`Table.h:67`、`ComboBox.h:62`、`Slider.h:20`、`CheckBox.h:29`、`Switch.h:19`、`TabControl.h:38`、`NavigationView.h:38` ……
- 事件不带载荷：`OnSelectionChanged(std::function<void()>)`（ListView/Table/ComboBox/TabControl/Segmented/GridView/TreeView/TreeTable 八个控件）不给索引，`TextBox::OnTextChanged()` 不给文本，`ListView::OnActivate()` 不给行。用户在回调里第一行永远是再查一次 `SelectedIndex()`。
- 由于不是 Signal，10.4 的 Property 绑定和 Command 都挂不上这些控件。
- `Connection` 析构不断开（`Signal.h:54` `~Connection() = default;`），只有 `ScopedConnection` 才 RAII。`auto c = btn.BindClick(...);` 出作用域后槽仍活着，与所有主流信号库（boost.signals2、sigslot、Qt）的直觉相反；而返回 `Connection` 的 `BindX` 恰恰是给"需要断开"的人用的。

建议：
- `Connection` 改为默认 RAII（析构即断开），需要"放手"的场景显式 `Release()`；或改名 `ConnectionHandle` 明示不管生命周期。二者取一，现在的命名 + 语义组合是陷阱。
- 全部改 `Signal<Args...>`；命名规范固定为：`OnX(fn) -> Self&`（链式、不关心断开）与 `BindX(fn) -> Connection`（需要断开时用），Button 已经是这个形态。
- 载荷补齐：`OnSelectionChanged(void(ptrdiff_t view, ptrdiff_t data))`、`OnTextChanged(void(std::wstring_view))`、`OnActivate(void(size_t data))`、`OnToggled(bool)` 已对。无参旧重载保留一版（内部包一层）。
- `Signal::Emit` 期间禁止重入同一 Signal 的 `Connect` 触发 `slots_` 重分配后指针失效——现在拷到栈上已经安全，但 `>8` 个槽走 `std::vector` 会在绘制路径外分配，注明即可。

### 10.4 Property 只有半截

现状：`Property<T>` 有 `Changed` 信号，但没有任何控件提供 `Bind(Property&)`（`grep BindText|BindValue` 只命中 `Table::BindTextBox`，那是列绑定不是属性绑定）。Gallery 的 `SetIntensity`（`common.cpp:160-193`）是没有绑定的直接后果：一个 float 手工同步 Label / Slider / NumberBox / 5 个 Swatch / 3 个 RadioButton / Switch 共 12 个控件，每个前面一个 `if (ptr)` 判空，且要靠 `fabs(...) > 0.5` 防回环。

建议（依赖 10.3）：
```cpp
lumen::Property<float> glow{0.5f};
label.BindText(glow, [](float v) { return std::format(L"{}%", int(v * 100)); });
slider.BindValue(glow, 100.0f);           // 双向；scale 参数解决 0..1 ↔ 0..100
number.BindValue(glow, 100.0f);
sw.BindChecked(glow, [](float v) { return v > 0.01f; });   // 单向派生
card.BindVisible(ready);                   // Control 层：BindVisible / BindEnabled
```
- 双向绑定内部用"写入前比较 + 抑制回环标记"，控件析构自动断开（`ScopedConnection` 成员，与 `Button::cmd_changed_` 同法）。
- `Computed<T>` 派生属性（`Computed<bool> ready{[&]{ return glow > 0; }, glow}`）。
- `Property<T>::Persist(Settings&, key)` 见 10.13。

### 10.5 三处"必须绕库"的地方

**焦点**：`Control::SetFocus()` 是 protected（`Control.h:287`）。应用想把焦点放到搜索框只能派生子类——Gallery 的 `SuggestHost::FocusSelf`（`common.h:27-30`）就是为此存在；库自己也在绕：`Table::CellEditor::FocusCaret`（`Table.h:316`）。建议 `Control::Focus()` 公开，加 `Window::FocusFirst()` / `FocusNext(bool backwards)`，`Window::Focused()` 已有。

**应用级动画**：没有公共时钟入口。Gallery 造了一个不可见的 `JobClock : Control`（`common.h:52-71`），空 `Measure/Draw`、`HitTransparent`，唯一目的是拿到 `OnAnimate`。建议：
```cpp
Connection Window::OnFrame(std::function<bool(float dt)> fn);   // 返回 false 自动断开、停拍
// 或更高一层：
void Window::Animate(Tween& tween, std::function<void(float)> apply);
```
语义与控件时钟一致：只在有活跃回调时驱动，不常驻。

**窗口图标**：`gallery/main.cpp:15-37` 用 `FindResourceW/LoadResource/LockResource/LoadImageW/SendMessageW(WM_SETICON)` 共 20 行原生 Win32；`TitleBar::LoadIconMemory` 只管自绘标题栏，任务栏/Alt-Tab 图标还要另走一条路。`Window::TrayIcon(void* hicon, ...)`（`Window.h:135`）的 `void*` 也是同一问题。建议统一 `IconSource`：
```cpp
window.Icon(L"IDI_APP");                          // 资源名或 .ico 路径
window.Icon(std::span<const std::byte> ico);       // 内存
window.TrayIcon(IconSource, tooltip);              // 同一套
```
一次设齐 `ICON_BIG/ICON_SMALL` + TitleBar + 托盘。

### 10.6 NavigationView 与 PageHost 没接线

现状：最常见的应用壳 = NavigationView + ScrollViewer + PageHost，但三者靠用户在 `OnSelectionChanged` 里手写（`main.cpp:85-92` + `common.cpp:244-248`）：`host.Show(id)`、`scroll.ScrollToY(0)`、还要 3 个全局指针 `g_shell_nav/g_scroll/g_page_host` 才能跨文件跳页。Footer 的 "GitHub" 项是动作不是页面，点了以后要手工 `nav.SetSelectedId(CurrentPage())` 把选中恢复回去。

建议：
- `nav.BindPages(PageHost& host)`：选中 id 即 `host.Show(id)`，若 host 外层是 ScrollViewer 则自动回顶；`nav.Page(id)` 直接返回该页 `StackPanel&`（把 Gallery 的 `add_page` lambda 收进库）。
- `NavigationItem.action = true`：点击只触发 `OnItemInvoked(id)`，不改变选中，不切页。
- `NavigationView::Navigate(id)` = 设选中 + 切页 + 触发回调，替代 Gallery 的 `ShowPage`。

### 10.7 Dialog 生命周期与自定义内容

现状：`Window::ShowDialog(Dialog&)` 要求调用方持有对象活到关闭，Gallery 因此用全局 `g_dialog` + `g_dialog_name`（`common.cpp:14/32/235-238`），并且用 `if (!g_dialog_name)` 防止重复 Add。`ShowDialog(DialogSpec)` 只能 title + message，放不进自定义控件。

建议：
- `Window::ShowDialog(std::unique_ptr<Dialog>)`：窗口接管所有权，关闭后销毁。
- `DialogSpec.content = std::function<void(Panel&)>`：在正文与页脚之间建自定义内容。
- 两个最常用的一行版：
```cpp
std::future<bool> Window::Confirm(std::wstring_view title, std::wstring_view message,
                                  std::wstring_view ok = L"确定", std::wstring_view cancel = L"取消");
void Window::Alert(std::wstring_view title, std::wstring_view message);
std::future<std::optional<std::wstring>> Window::Prompt(title, message, placeholder);
```
`Dialog::GetFuture()` 已有，这只是把它包起来。

### 10.8 字符串：只有 `wchar_t`

现状：公共 API 只接受 `std::wstring_view`；`lumen::U8()`（`Core.h:96-132`）每次调用堆分配一个 `wstring`，且 40 行 UTF-8 解码 inline 在 `Core.h`，进了每个控件头的 include 链。UTF-8 代码库（JSON、网络、跨平台业务层）的每个 `Text()` 都要包一层。

建议：
- 文本 setter 增加 `std::string_view` 重载（`Text(std::string_view utf8)`），在 Control 层统一转换，短串走栈缓冲（`MultiByteToWideChar` 到 256 wchar 栈数组，超长才分配）。
- `U8` 搬到 `lumen/Text.h`，`Core.h` 只留几何与颜色。
- 提供 `operator""_w`（`u8"你好"_w`）供字面量场景。

### 10.9 入口与 `windows.h`

现状：`examples/template/main.cpp:7-9` 必须 `#include <windows.h>` 并写 `int WINAPI wWinMain(HINSTANCE, HINSTANCE, LPWSTR, int)`。一个把 HWND 都藏起来的库，用户第一行代码却是 Win32 签名。

建议：
```cpp
// 用户代码
int lumen_main(std::span<const std::wstring_view> args) { lumen::App app; ... return app.Run(); }
```
库里提供 `wWinMain` 实现（小 target `lumen::main`，或头文件宏 `LUMEN_MAIN(lumen_main)`），命令行用 `CommandLineToArgvW` 拆好。`Painter.h` 去 `windows.h` 在 5.4 已列。

### 10.10 诊断与防呆

现状：
- 零调试期校验：`Panel::Child(size_t)`（`Panel.h:55`）、`ComboBox::ItemAt`（`ComboBox.h:35`）越界直接 UB；跨线程调用控件方法无检测（只有 `Window::IsUiThread()` 让用户自查）；`Add(std::unique_ptr<Control>)` 路径下把已有父级的控件再 Add 一次不会报错；`Command` / `ItemsModel` 先于控件析构是悬垂，README 只写"须活过控件"。
- `log.h` 在 `src/core`，不是公共头：用户无法开日志，也无法接管输出（`LogEnable` 只写文件）。
- 悬垂引用没有任何护栏：`Add<T>()` 返回的 `T&` 与 `Ref(T*&)` 写出的指针在 `Remove/Clear` 后即野指针（`control.cpp:365-380` 只做 `ForgetTree`，不回写 `Ref` 槽）。`Control` 允许 move（`Control.h:66-67`），但入树后再 `std::move` 会破坏父子链，头注释只写"不要"（`Control.h:159-160`），没有检查。动态增删的界面（列表项卡片、向导页）几乎必踩。

建议：
- `Ref` 槽在 `Remove/Clear` 时回写 `nullptr`（`ref_slot_` 已存着地址，成本为零）；Debug 下 move 构造/赋值断言 `parent_ == nullptr`。
- 提供 `WeakRef<T>`（控件析构时置空，`Control` 内一个侵入式链表头即可，热路径零成本），Gallery 的十几个全局裸指针全换成它做示范。
- `LUMEN_DEBUG_CHECKS`（Debug 默认开，Release 关）：越界 / 跨线程 / 重复父级 / 未入树时 `Ref` → `Log` + `__debugbreak()`。
- 公共 `lumen/Log.h`：`SetLogSink(std::function<void(LogLevel, std::wstring_view)>)`，默认 `OutputDebugString`。
- `Window::DumpTree(std::wostream&)`：类型 / bounds / visible / enabled / focus 缩进输出；Gallery 绑 F12。
- 模型/命令析构安全：`ItemsModel::~ItemsModel` 发 `Detached` 信号，控件收到后置空 `model_`；`Command` 同理。用户忘了生命周期最多是列表变空，不是崩。

### 10.11 头文件与构建体验

- `Control.h` 因为内嵌 `Menu context_menu_` 而 `#include "Menu.h"`，Menu.h 再拉 `<cwctype>/<functional>/<vector>`，每个控件头都背着它。配合 7.2 冷数据外移后，`Control.h` 只需前置声明 `class Menu`。
- `Panel::Add<T>` 把 `sizeof(T)` 编进调用方 TU；`CMakeLists.txt:223-243` 已经为此打了 `OBJECT_DEPENDS` 补丁。这不只是依赖追踪问题——任何控件加一个私有成员，用户所有 TU 全部重编。长期方向是控件 PIMPL（`NavigationView` 已是 `Impl`），`Add<T>` 走 `MakeControl<T>()` 工厂。
- 作为子项目（`add_subdirectory` / FetchContent）时仍会构建 gallery 与三个 test，并探测同级 `../lumatext`（`CMakeLists.txt:15-21`）。建议 `LUMEN_BUILD_EXAMPLES/LUMEN_BUILD_TESTS` 默认 `PROJECT_IS_TOP_LEVEL`，LumaText 同级探测同理。
- 三条集成路径 target 名不一致：`add_subdirectory` 链 `lumen`（`examples/template/CMakeLists.txt:9`），`find_package` 链 `lumen::lumen`（README），主 `CMakeLists.txt` 没有 `add_library(lumen::lumen ALIAS lumen)`。用户从子目录切到安装包时要改链接名。加一行 ALIAS 即可。
- LumaText 的 DLL 拷贝逻辑 `lumen_enable_lumatext(target TRUE)`（`CMakeLists.txt:68-93`）只给库内四个可执行文件调用，外部消费者链上 `lumen` 后运行时找不到 `lumatext.dll`，静默回退 DirectWrite——用户以为自己在用 LumaText 其实没有。建议导出公开函数 `lumen_copy_runtime(target)`，并在 `lumenConfig.cmake` 里补 `INTERFACE_COMPILE_FEATURES cxx_std_20` 与可选依赖说明。
- 工作区里还有 22 个旧名头 `include/fluentui/*.h`（未跟踪，`git ls-files` 为 0）。IDE 补全会把 `fluentui/Button.h` 提给用户，删掉。

### 10.12 数据控件 API 收敛

现状：四种供数方式并存——ListView `ItemText(provider)`、Table `CellText(provider)`、TreeView 数据回调 / `SetFlatData`、ComboBox `AddItems` 值拷贝；`Bind(ItemsModel&)` 只有 ListView / Table 接了。取回选中项：ListView / Table 有 `SelectedIndex`（视图）+ `SelectedDataIndex`（数据）双空间，ComboBox 没有；`VectorModel<T>::At(i)` 存在但没有从控件直达 `T` 的路径，用户要 `model.At(list.SelectedDataIndex())` 自己拼。

建议：
- ComboBox / AutoSuggestBox / GridView 接 `Bind(ItemsModel&)`；TreeView / TreeTable 接 `TreeModel`（`ChildCount(parent)` / `Child(parent, i)` / `Get(id, ItemRow&)`）。
- 所有选择类控件统一 `SelectedIndex()`（视图）/ `SelectedDataIndex()`（数据）/ `OnSelectionChanged(view, data)`。
- `VectorModel<T>::Selected(const ListView&) -> const T*`、`Selected(const Table&)`：一次拿到 `T`，内部走 `SelectedDataIndex + SourceIndex`。
- `VectorModel<T>` 对非 `wstring`/`ItemData` 的 `T` 在没调 `Map` 时静默写空（`ItemsModel.h:109-118`），结果是"列表有行数但全空白"，很难定位。改为 `static_assert` 要求 `T` 可默认映射或已提供 `Map`；或构造函数必传映射函数。
- "模型须活过控件"与控件所有权方向相反（控件归父级，模型归用户），局部 `VectorModel` 绑到长寿命 ListView 是典型悬垂。除 10.10 的析构通知外，提供 `Bind(std::shared_ptr<ItemsModel>)` 重载让控件可共享持有。

### 10.13 常见任务的"一行版"

- **确认 / 提示 / 输入框**：见 10.7。
- **打开 URL / 资源管理器定位 / 打开文件所在目录**：Gallery footer 的 GitHub 项目前只能 Toast。补 `lumen::shell::OpenUrl(wstring_view)`、`RevealInExplorer(path)`、`OpenWithDefault(path)`（`ShellExecuteW` 包一层，放 `dialogs.cpp` 旁）。
- **后台任务**：`Window::Post` 已有，但"起线程 → 完成回 UI"仍要手写 `std::thread` + `Post` + 窗口已关闭判断。补：
```cpp
template <class R> void Window::RunAsync(std::function<R()> work, std::function<void(R)> then);
```
内部 `std::jthread` + `Post`，窗口关闭后丢弃 `then`；配 `ShowBusy` 自动开合的重载。
- **设置持久化**：`RememberPlacement` 已走注册表；补 `lumen::Settings settings(L"Software\\App")` 的 `Get/Set<wstring|int|bool|double>`，`Property<T>::Persist(settings, L"volume")` 读初值 + 写回。
- **剪贴板**：`clipboard::SetText/GetText` 已有；补 `SetFiles/GetFiles`（CF_HDROP）、`SetImage(bgra, w, h)`（CF_DIBV5）。

### 10.14 可发现性

- 没有生成式 API 文档，头注释质量参差：`ListView.h:73` 把多选快捷键写进了注释（好），但 Button/TextBox/Table 都没有"响应哪些键"的说明；事件列表要靠翻整个头。建议每个公共头顶部固定三段：`// Events:`、`// Keys:`、`// Layout:`（是否 Grow 友好、默认尺寸、是否 FillCross），并用脚本检查存在性。
- 公共 API 里的魔法数字：Toast `duration = 2.4f`（`Window.h:57`）、ToolTip "约 600ms"（`Control.h:80` 注释）、StatusBar 高度 28、ProgressBar 值域 0..1 只在注释里。建议进 `Theme`（`toast_duration`、`tooltip_delay`）或强类型 `Seconds{2.4f}`，让 IDE 能跳转到定义。
- README 缺"推荐 include 集"速查表：窗口壳（App/Window/NavigationView/PageHost）、表单（FormField/TextBox/ComboBox/Button/Dialog）、列表（ListView/Table/ItemsModel）三套最小头文件列表，避免新手默认 `lumen.h` 拖满编译。

### 10.15 推进顺序与验收

**先做（机械、影响面最大）**：10.1 CRTP 转发 → 10.2 命名统一 → 10.3 Signal 全覆盖。三项都是纯 API 整理，无渲染风险，做完后 10.4 才能开始。

**再做（消灭 Gallery 里的"绕库"）**：10.4 Property 绑定、10.5 Focus/OnFrame/Icon、10.6 BindPages、10.7 Dialog 所有权。验收标准就是 Gallery：`SuggestHost`、`JobClock`、`g_dialog`、`g_shell_nav/g_scroll/g_page_host`、`SetIntensity` 里的 12 个 `if (ptr)`——这五处全部删除，`main.cpp` 里的 20 行图标代码变成一行。

**后做**：10.8 UTF-8、10.9 入口、10.10 诊断与悬垂护栏、10.11 头文件与 CMake、10.12 数据模型、10.13 一行版、10.14 可发现性。

**验收方式**：
- 新增 `tests/api/chain_compile.cpp`：用 X-macro 列出全部控件，对每个生成 `panel.Add<T>().Margin(4.0f).SetVisible(true).SetToolTip(L"").Grow().SetEnabled(true)` 链，编不过即失败——这是 10.1 的回归测试，也强制新控件必须走 CRTP。
- `tests/api/naming.py`（或 CMake 阶段脚本）扫 `include/lumen/*.h`，出现 `Get[A-Z]\w*\(\)` 或同类同时存在 `X(v)` 与 `SetX(v)` 即报错。
- `examples/template/main.cpp` 不再包含 `<windows.h>`，行数 ≤ 20。
- Debug 构建下故意 `Child(999)`、跨线程 `Text()`、重复 `Add`，`lumen_visual_test` 新增三条断言确认命中 `__debugbreak`（测试里替换为抛异常）。

---

## 十一、开箱即用：最少代码拿到最好效果

第十节做完后，API 层面的"绕库"已经消灭。这一节回答另一个问题：一个**没看过 Gallery 源码**的人，从拿到仓库到跑出 Gallery 首页那种质感，中间还要踩几个坑、写几行不该他写的代码。判定标准只有一条：**默认值就是最佳效果，写多才是退化，而不是写多才够看。**

### 11.0 现状判定

已经算"开箱即用"的部分：`examples/template/main.cpp` 20 行跑起来；`lumen::main` 免 `wWinMain`；`Row/Column/Grid` 不用算坐标；`ShowToast/ShowDialog(DialogSpec)/RunAsync(busy)/BindShortcut/RememberPlacement/SingleInstance` 各一行；`Bind(ItemsModel)`、`Property` 绑定、`Command`、`Settings::Persist`；UIA、Per-Monitor DPI、触摸、IME 默认全开。

**还不算**的十处，全部有代码位置可查：

1. **默认外观不是 LUMEN 外观。** `Window` 默认 `Frame::System`（`Window.h:73`）+ `Backdrop::None` + 无图标。Gallery 首页那张脸要 `Frame::Client` / `Backdrop(All)` / `Icon(...)` / `MinSize(...)` 四行，模板里只写了一行。用户第一眼看到的是系统白标题栏配纯黑客户区。
2. **`Window::Confirm/Prompt` 返回 `std::future`（`Window.h:108/112`）是死锁陷阱。** 对话框在同一消息循环里运行，UI 线程上 `.get()` 永远等不到结果。这是全库"看起来最简单、实际最危险"的 API，README 没有一句警告。顺带 `Window.h` 因此 `#include <future>`，每个翻译单元都在付编译时间。
3. **内置字串硬编码中文。** `window_impl.cpp:454/465/466` 的 `确定/取消`、`busy_overlay.cpp:19` 的 `取消`。英文系统的用户 `Alert()` 弹出来是中文按钮。
4. **Property 绑定覆盖不全，最常见的一条恰好缺。** 现有 `BindText` 只在 Label（单向 + fmt），`BindValue` 在 Slider/NumberBox（仅 `Property<float>`），`BindChecked` 三个都要 `Pred`。表单最基本的 `TextBox ↔ Property<std::wstring>` 双向、`Switch ↔ Property<bool>` 直绑、`ComboBox ↔ Property<int>`、`DatePicker ↔ Property<year_month_day>` 全部没有，用户又得回到 `OnTextChanged` + 手工回写 + 防环。
5. **FormField 有 `Error()` 没有校验。** `FormField.h:40` 的 `Error(text)` 是纯显示，"必填/格式/长度"三种校验每个字段各写一次 `OnTextChanged`，再自己算"整表是否有效"去禁用提交按钮。
6. **Table 列绑定每列五个 lambda。** `Table.h:51-58` 的 `BindTextBox(col, get, set)` 一类接口对 `VectorModel<T>` 用户来说全是样板：`get = [&](i){ return m.At(i).name; }`，`set = [&](i, v){ m.At(i).name = v; }`。
7. **基类命名还剩最后一公里。** `naming.py` 白名单里 `SetVisible/SetEnabled/SetToolTip/SetSpotlight/SetContextMenu/SetAccessibleName/SetCard/SetCardStyle` 与全库 `X(v)` 风格不一致；新手在链里先写 `.Visible(false)` 编不过再改 `.SetVisible(false)`，每个人都要试一次。README 第 185 行还留着 `SetSelectedIndex(1)`（实际是 `SelectedIndex(1)`）——文档已经开始漂移。
8. **集成路径只有一条。** 模板 CMake 写死 `add_subdirectory(../..)`，独立目录拿不到；没有 FetchContent 片段、没有 vcpkg port、没有预编译包；`lumen_copy_runtime` 要用户记得调（模板自己就没调）。LumaText 缺失静默降级 DirectWrite（`find_package(LumaText QUIET)`），用户不知道为什么自己的字比 Gallery 截图"糙"。
9. **忘构造 `App` 没有任何提示。** `App()` 负责 DPI 感知（`app.cpp:112`）；先建 `Window` 再建 `App`（或干脆不建）时窗口照样出来，只是高 DPI 下模糊——最难排查的那种"看起来能跑"。
10. **失败不可见。** 渲染设备走 WARP 兜底（`renderer.cpp:42`）不告知；字体回退不告知；`LUMEN_LOG` 环境变量只在 `log.cpp:45` 出现。首帧 `Root()` 空着（忘了 `Add`）显示纯黑，用户以为渲染坏了。

### 11.1 零配置默认 = Gallery 效果

- `Window` 构造重载：`Window(std::wstring_view title)`。默认 `Frame::Client`、`Backdrop::All`、客户区 960×640 屏幕居中、`MinSize` 取默认尺寸的 60%、窗口图标自动取 exe 第一个 `RT_GROUP_ICON`（`EnumResourceNamesW` 拿到第一个 id 后走现有 `SetIcon(int)`；没有资源就不设）。现有三参构造保留给要系统边框的人。
- `WindowSpec` 聚合初始化替代位置参数：`Window w({.title = L"演示", .size = {800, 600}, .frame = Frame::System});`，与 `DialogSpec/ToastData` 同一风格，IDE 补全能看到全部选项。
- `App` 自动化：`Window` 构造里调 `App::Ensure()`（幂等：DPI 感知 + 窗口类）；`App` 对象保留给 `Run()`。再进一步给一行入口：

```cpp
// 之前：App / Window / Backdrop / Show / Run 五行固定样板
int lumen_main(std::span<const std::wstring_view>) {
    return lumen::Run(L"演示", [](lumen::Window& w) {
        w.Root().Add<lumen::Button>(L"你好", lumen::ButtonKind::Primary);
    });
}
```

`Run` 内部就是 `App app; Window w(title); build(w); w.Show(); return app.Run();`。模板从 20 行降到 8 行，且首屏等于 Gallery 质感。

### 11.2 消灭 future 死锁

- 删 `std::future` 版 `Confirm/Prompt`，换回调：`Confirm(title, message, std::function<void(bool)> then, ok = {}, cancel = {})`、`Prompt(title, message, std::function<void(std::optional<std::wstring>)> then, placeholder = {})`。`Dialog::Future()` 同样删除（`Dialog.h:66`），`Window.h/Dialog.h` 去掉 `<future>`。
- 可选（C++20 已开）：`lumen::Task` 协程 + `co_await w.Confirm(...)`，恢复点用现有 `Window::Post` 回 UI 线程。这是真正"最少代码"的形态：

```cpp
lumen::Task OnDelete(lumen::Window& w, Model& m) {
    if (!co_await w.Confirm(L"删除", L"不可恢复")) co_return;
    auto n = co_await w.Async([&] { return m.Delete(); }, L"正在删除…");   // RunAsync + busy
    w.ShowToast(std::format(L"已删除 {} 项", n), lumen::ToastKind::Success);
}
```

先做回调版（机械、无风险），协程作为 `lumen/Task.h` 独立头，不进 `Window.h`。

### 11.3 内置字串表

- 新增 `lumen::Strings`（`ok/cancel/close/yes/no/today/clear/search_placeholder/busy_cancel/empty_state_default`），默认按 `GetUserDefaultUILanguage()` 在 zh-CN / en-US 之间选，`App::Strings(Strings)` 整表覆盖。
- 替换点：`window_impl.cpp:454/465/466`、`busy_overlay.cpp:19`；grep `L"[\u4e00-\u9fff]` 扫 `src/` 作为 `naming.py` 的新检查项，防止再混进硬编码。

### 11.4 表单一行绑定

- 补齐双向绑定（都是现有 `Slider::BindValue` 的同款实现：`OnChanged` 进 `ScopedConnection`，控件事件回写，值相等短路防环）：
  - `TextBox/PasswordBox/AutoSuggestBox::BindText(Property<std::wstring>&)`；`TokenBox::BindTokens(Property<std::vector<std::wstring>>&)`
  - `Switch/CheckBox/ToggleButton::BindChecked(Property<bool>&)` 无 `Pred` 重载
  - `ComboBox/Segmented/TabControl::BindSelectedIndex(Property<int>&)`
  - `Rating::BindValue(Property<int>&)`、`Slider/NumberBox::BindValue(Property<double>&)`、`DatePicker/TimePicker::BindValue(Property<std::chrono::...>&)`
- `FormField::Validate(std::function<std::wstring(std::wstring_view)>)`：子级是 TextBox 家族时自动挂 `OnTextChanged`，返回空串即清错。预置 `validate::Required()` / `MinLength(n)` / `Pattern(re)` / `Range(lo, hi)` 可 `|` 组合。
- `Form` 容器（`Column` 派生）：`form.Valid()` 返回 `Computed<bool>`，直接 `submit.BindEnabled(form.Valid())`。

```cpp
// 之前：每字段 OnTextChanged + Error + 一个全局 "都合法吗" 函数，约 25 行
lumen::Property<std::wstring> name, mail;
auto& form = root.Add<lumen::Form>();
form.Field(L"名称").Validate(validate::Required()).Add<lumen::TextBox>().BindText(name);
form.Field(L"邮箱").Validate(validate::Required() | validate::Pattern(L".+@.+")).Add<lumen::TextBox>().BindText(mail);
form.Add<lumen::Button>(L"提交", lumen::ButtonKind::Primary).BindEnabled(form.Valid());
```

- Table 成员指针列：`table.Bind(model).Column(L"名称", &Item::name).Column(L"完成", &Item::done).Column(L"进度", &Item::progress)`。类型即列种类：`std::wstring` → Text（可编辑写回 `model.At(i)` 并 `NotifyChanged`）、`bool` → CheckBox、`float` → Progress、`std::function` → Button。`CellText/BindTextBox` 一族保留给非 `VectorModel` 数据源。

### 11.5 三种拿到库的方式，一种都不用记步骤

- README 第一屏放 FetchContent 六行；模板改为独立工程（`FetchContent_Declare(lumen GIT_REPOSITORY ...)`，不再 `add_subdirectory(../..)`），附 `app.rc` + `app.ico`，让"图标从哪来"有现成答案。
- `lumen_add_executable(name src...)`：一个函数包掉 `WIN32`、链 `lumen::lumen lumen::main`、`lumen_copy_runtime`、`/utf-8`。用户 CMake 只剩 `find_package(lumen CONFIG)` + 这一行。
- `ports/lumen/`（vcpkg overlay）+ GitHub Release 挂预编译包（`lib/lumen.lib lumen_main.lib` + `include/` + `bin/lumatext.dll`）。
- LumaText 缺失：configure 阶段 `message(WARNING "LumaText not found; text falls back to DirectWrite")` 一次；新增 `LUMEN_REQUIRE_LUMATEXT` 选项让缺失直接失败；运行时 `Log(Warn)` 一条。
- 非 CMake 用户：提供 `LUMEN_MAIN()` 宏（展开为 `wWinMain` 转发到 `lumen_main`），VS 工程直接加源码也不用碰 `/ENTRY`。

### 11.6 命名最后一公里

- `Control` 基类：`SetVisible/SetEnabled/SetToolTip/SetSpotlight/SetContextMenu/SetAccessibleName` → `Visible/Enabled/ToolTip/Spotlight/ContextMenu/AccessibleName`；`Panel::SetCard/SetCardStyle` → `Card/CardStyle`。用 `tools/drop_set_writers.py` 一次改完，`ControlOf/PanelOf` 转发同步；`naming.py` 白名单缩到 `SetBounds/SetChildBounds`（布局内部）、`Set*Sink/Handler`（全局钩子）与 `AutomationSet*`（UIA 协议名）。
- 新增 `tests/api/readme_compile.cpp`：脚本从 `README.md` 抽出全部 ```cpp 块拼成一个编译单元（每块包进独立函数），编不过即失败。这一条能立刻抓到第 185 行的 `SetSelectedIndex`，之后 README 与 API 不会再漂移。

### 11.7 失败可见

- Debug：`Window` 构造时 `App::Ensure()` 之后仍检测到进程 DPI 感知不是 PMv2（用户自己的 manifest 覆盖了）→ `DebugTrap`；首次 `Show()` 时 `Root().ChildCount() == 0` → `Log(Warn, "empty root")`；渲染设备落到 WARP → `Log(Warn)`；字体族回退 → `Log(Info)` 一次。
- `LUMEN_LOG=path` 与 `SetLogSink` 写进 README「排障」小节，`lumen_gallery` 的 F12 `DumpTree` 也一并写进去。

### 11.8 一页速查

README 顶部 60 行内给齐：三套最小 include 集（窗口壳 / 表单 / 列表，对应 10.14 的欠账）；布局心智模型三句话（Column 纵向 Stretch、`Grow` 吃主轴剩余、超出用 `ScrollViewer().Grow()`）；十个最常用的一行 API（`ShowToast / Confirm / RunAsync(busy) / BindShortcut / Persist / Bind(model) / Command / ShowDialog(spec) / RememberPlacement / SingleInstance`）。控件头顶部三段注释已到位，不再另建文档站。

### 11.9 推进顺序与验收

**先做（一天内、零渲染风险）**：11.2 回调版 Confirm/Prompt → 11.3 字串表 → 11.6 基类命名 + README 编译测试。三项都是纯 API 与文案。

**再做（决定"首屏质感"）**：11.1 `Window(title)` 默认值 + `App::Ensure` + `lumen::Run` → 11.5 模板独立化 + `lumen_add_executable` → 11.7 三条 Log。

**后做（决定"表单/表格代码量"）**：11.4 绑定补齐 → `FormField::Validate` / `Form` → Table 成员指针列 → 可选协程。

**验收**：
- `examples/template/main.cpp` ≤ 10 行、带图标、首屏与 Gallery Overview 同一质感；模板目录 `cmake -S . -B build` 独立可构建。
- `lumen_api_test` 新增：README 全部代码块编译通过；`src/` 无 `L"中文字面量"`；`include/` 里 `Set[A-Z]` 只剩白名单三类。
- `lumen_visual_test` 新增：`Window(title)` 默认帧的标题栏区域亮度 / 顶部环境辉光断言（证明默认值就是 `Client + All`）。
- Gallery 里 `Overview` 的强度滑块、`Input` 页表单、`Collections` 页 Table 三处示例改用 11.4 的一行绑定后，各删掉的行数写进 PR 描述；任何一处没有变短，说明该 API 设计还没到位。
